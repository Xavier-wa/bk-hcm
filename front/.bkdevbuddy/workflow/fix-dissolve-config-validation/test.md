# fix-dissolve-config-validation 测试清单

> 工作流: `fix-dissolve-config-validation`
> 关联文档: Coding（lite 链无 PRD/Design/API）
> 负责人: 开发自测 / QA

## 验证范围

- 功能边界: 裁撤配置弹窗「裁撤时间段与项目类型」可编辑表格的必填校验引用生命周期（新增 / 删除 / 重置 / 取消重开），保存不再误报「请检查必填项」；列级校验错误提示恢复正常。
- 不在范围: 后端接口契约、裁撤额度调整表（未改动）、裁撤总览/明细列表其它功能、其它模块回归。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不写入具体域名>`
- 测试账号: `<test_account>`（占位，不写真实账号）
- 数据准备:
  - 测试环境已存在至少 1 个裁撤时间段配置（含 1 行项目类型）
  - E2E 自动化使用本地 mock（`e2e/mocks/api.ts` 有状态 + 深拷贝），无需真实后端

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 新增空行后重置再保存不误报 | 已打开裁撤配置弹窗 | 新增一行不填必填 -> 重置 -> 保存配置 | 不提示必填项，保存成功 | 无需（未提交脏数据） |
| P0-02 | 新增空行后取消重开再保存不误报 | 已打开裁撤配置弹窗 | 新增一行不填必填 -> 取消 -> 重新打开 -> 保存配置 | 行数恢复初始，不提示必填项，保存成功 | 无需 |
| P0-03 | 新增空行直接保存仍拦截并定位 | 已打开裁撤配置弹窗 | 新增一行不填必填 -> 保存配置 | 提示「请检查必填项」，新行项目类型列展示校验错误 | 补充选择项目类型后保存 |
| P0-04 | 原有保存主链路正常 | 已打开裁撤配置弹窗 | 新增一行并选择新项目类型 -> 保存配置 | 保存成功，弹窗关闭，总览搜索下拉包含新类型 | 删除该配置行 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 删除中间行后保存正常 | 时间段内至少 2 行项目类型 | 删除第一行 -> 保存配置 | 保存成功，不误报必填项 |
| P1-02 | 多个时间段删除/重置后保存正常 | 至少 2 个时间段 | 删除一个时间段或点重置 -> 保存配置 | 保存成功，不误报必填项 |
| P1-03 | 日期范围未选时保存拦截 | 时间段 start/end 为空 | 保存配置 | 提示必填项，裁撤时间列展示校验错误 |

### P2 - UI 细节（按需）

- [x] 新增空行直接保存后，行内错误提示（红框/图标）可见且文案为「请选择项目类型」
- [x] 重置/取消重开后无残留行，表格行数与配置一致
- [x] 弹窗连续开关多次后保存仍正常（无引用累积）

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 本地 mock E2E 覆盖（`e2e/specs/dissolve.spec.ts` 新增空行后重置再保存） |
| P0-02 | PASS | 本地 mock E2E 覆盖（新增空行后取消关闭再打开） |
| P0-03 | PASS | 本地 mock E2E 覆盖（新增空行直接保存：提示 + 行内 `.is-error` 展示） |
| P0-04 | PASS | 本地 mock E2E 覆盖（原保存主链路用例） |
| P1-01 | PASS | 用户手测通过 |
| P1-02 | PASS | 用户手测通过 |
| P1-03 | PASS | 用户手测通过 |

- 执行人: <DEVELOPER_NAME>（用户本人手测）
- 执行日期: 2026-08-10
- 总体结论: PASS
- 后续行动: 直接合入；TAPD bug 单停在 for_test，待测试验证后人工关单

### E2E 自动化结论（verify-e2e）

- 执行命令: `node node_modules/@playwright/test/cli.js test --config e2e/playwright.config.ts e2e/specs/dissolve.spec.ts`
- 执行日期: 2026-08-10
- 结果: PASS（4/4）
- 覆盖: P0-01 / P0-02 / P0-03 / P0-04
- 报告: `front/e2e/playwright-report/`（本地生成，未入库）
