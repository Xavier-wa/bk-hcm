---
description: "bkui-vue3（蓝鲸 BK UI）组件使用规范与 MCP 文档查询指引。当任务涉及以下任一情况时必须主动读取本规则：(1) 使用、配置或修改任意 bk- 开头的组件——bk-table、bk-table-column、bk-form、bk-form-item、bk-input、bk-select、bk-cascader、bk-dialog、bk-sideslider、bk-tree、bk-upload、bk-button、bk-checkbox、bk-radio、bk-tab、bk-popover、bk-loading 等；(2) 写表格列定义、column.render、表格多选、全选、行禁用、useTableSelection；(3) 不确定 bkui 组件的 prop / 事件 / 插槽 / 类型 / 默认值时（应通过 MCP 工具 get-component / get-components-batch / get-component-list 查询，而不是猜测）；(4) 想通过覆盖样式实现某个交互效果时（先确认是否有内置 prop）；(5) 用户提到\"bk 组件\"\"蓝鲸组件\"\"BK UI\"\"bkui\"\"组件 API 文档\"\"组件 prop\"\"组件事件\"等。"
globs: ["front/**/*.vue", "front/**/*.tsx"]
alwaysApply: false
---

# bkui-vue3 组件使用规范

## 核心原则

使用 bkui-vue3 组件时，不要猜测组件能力，在适当时机主动查阅 MCP 文档。

## 必须查阅文档的场景

### 1. 配置属性选择

当需要配置组件属性时，应先查阅文档确认：

- 是否有对应的 prop 可用
- prop 的类型和可选值
- 是否有更好的内置方案，而非自己写样式覆盖

```typescript
// ❌ 错误：猜测属性存在
<bk-form-item spacing="small">  // spacing 可能不存在

// ✅ 正确：先查阅文档确认属性
// 调用 MCP: get-component { name: "form" }
```

### 2. 复杂组件使用

以下组件使用时应主动查阅：

- `bk-table` - 列配置、分页、排序等
- `bk-form` / `bk-form-item` - 表单验证、布局
- `bk-tree` - 树形数据、异步加载
- `bk-dialog` / `bk-sideslider` - 弹窗配置
- `bk-upload` - 上传配置
- `bk-select` / `bk-cascader` - 远程搜索、多选等

### 3. 不确定的用法

- 不确定某个功能是否有内置支持
- 需要自定义样式来实现某个效果时（可能有更好的方案）
- 遇到组件行为不符合预期时

### 4. 事件和插槽

- 需要监听组件事件时，确认事件名称和参数
- 需要使用插槽时，确认插槽名称和作用域

## 不需要查阅的场景

- 简单的基础组件使用（button 点击、input 绑定 v-model）
- 当前会话中已经查阅过的组件
- 对组件用法非常确定时

## 查阅方式

使用 bkui-vue3 MCP 工具：

```
// 查询单个组件
get-component { name: "form" }

// 批量查询相关组件
get-components-batch { names: ["form", "input", "select"] }

// 获取组件列表
get-component-list {}
```

## 实践建议

1. **宁可多查一次，不要猜错实现** - 查阅文档的成本远低于错误实现后修复的成本
2. **发现组件能力不足时，先确认** - 可能是你不知道而非组件不支持
3. **避免不必要的样式覆盖** - 如果需要覆盖样式实现功能，先确认是否有内置方案

## bk-table 使用规范

### Column render 函数

在 `bk-table-column` 的 `render` 函数中，**必须使用 `row` 而不是 `data`** 来访问行数据。

**原因**：`data` 参数是非响应式的，`row` 参数是响应式的。

```typescript
// ❌ 错误：使用 data
const columnConfig = {
  name: {
    render: ({ data }) => data.name,
  },
};

// ✅ 正确：使用 row
const columnConfig = {
  name: {
    render: ({ row }: { row?: IDataItem }) => row.name,
  },
};
```

### 表格选择功能

使用 `useTableSelection` hook 处理表格多选，而不是手写选择逻辑。

```typescript
// ❌ 错误：手写选择逻辑
const selectedItems = ref([]);
const handleSelectionChange = ({ checked, row, isAll }) => {
  if (isAll) {
    selectedItems.value = checked ? tableData.value : [];
  } else if (row) {
    // ... 手动处理
  }
};

// ✅ 正确：使用 useTableSelection
import useTableSelection from "@/hooks/use-table-selection";

const isRowSelectable = ({ row }) => {
  return row.status === "init"; // 根据业务逻辑判断
};

const {
  selections, // 已选择的项
  checked, // 已选择项的 id 列表（用于表格默认勾选）
  handleSelectChange, // 单个选择事件处理
  handleSelectAll, // 全选事件处理
  resetSelections, // 重置选择
} = useTableSelection({ isRowSelectable });
```

**模板绑定**：

- `:is-row-select-enable` - 控制行 checkbox 禁用状态（与 hook 的 isRowSelectable 使用同一个函数）
- `@select-all` 和 `@selection-change` - 绑定 hook 返回的事件处理函数

```vue
<bk-table
  :data="tableData"
  :is-row-select-enable="isRowSelectable"
  @select-all="handleSelectAll"
  @selection-change="handleSelectChange"
/>
```

**注意**：`isRowSelectable` 同时传递给 hook 和 table，确保选择逻辑和 UI 禁用状态一致。