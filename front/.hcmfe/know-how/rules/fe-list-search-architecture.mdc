---
description: '列表页 + 搜索栏代码组织架构规范。当任务涉及以下任一情况时必须主动读取本规则：(1) 新建列表页、新增带搜索栏的列表模块、创建 data-list 或 search 子组件；(2) 修改或重构现有列表页结构，调整 index.vue / data-list.vue / search.vue / column.ts / condition.ts 的职责划分；(3) 代码中出现 children/data-list、children/search、@Model、@Column、getModel、getProperties、transformSimpleCondition、useSearchQs、column-factory、condition-factory、ModelPropertyType、ModelPropertyColumn、ModelPropertySearch、display-value、hcm-search-、meta.display.appearance、meta.search.op、AppearanceType 等符号；(4) 配置 @Column 的 type 参数（string/enum/datetime/list/user/business 等）、配置 meta.display 外观（dynamic-status/tag/link 等）、配置 meta.search 运算符；(5) 用户提到「列表页」「搜索栏」「表格列定义」「搜索条件」「children 目录」「列表组件拆分」「列表页架构」「display-value 联动」「模型驱动渲染」「工厂模式列定义」等。'
globs:
  - 'front/**/*.vue'
  - 'front/**/*.ts'
  - 'front/**/*.tsx'
alwaysApply: false
---

# 列表页 + 搜索栏代码组织架构规范

## 核心原则

凡包含「搜索栏 + 数据列表」的页面，**必须**采用 `children/` 目录组织模式，将容器逻辑与展示组件分离，将列定义 / 搜索条件定义与组件模板分离。禁止把所有逻辑塞进单个 `index.vue`，也禁止把列定义 / 搜索条件写成内联配置。

---

## 1. 标准目录结构

```
xxx/list/                        # 列表模块根目录（xxx 为业务模块名）
├── index.vue                    # 容器组件：状态管理 + 数据请求 + 业务编排
└── children/                    # 子组件目录
    ├── data-list/               # 数据列表子模块
    │   ├── data-list.vue        # 纯展示表格组件（props down, events up）
    │   └── column.ts            # 表格列模型定义（@Model + @Column 装饰器）
    └── search/                  # 搜索栏子模块
        ├── search.vue           # 纯展示搜索表单组件（props down, events up）
        └── condition.ts         # 搜索条件模型定义（@Model + @Column 装饰器）
```

### 1.1 参考实现

- 简单场景（单资源类型，列表 + 搜索）：`front/src/views/resource-plan/gpu/list/`
- 复杂场景（多资源类型，工厂模式）：`front/src/views/task/`
- 多 Tab 场景（多个独立列表页共享页级容器）：`front/src/views/server-dissolve/`
- 标准 search.vue + conditionInitValues 模式：`front/src/views/cloud-account-manage/cloud-secret/`

---

## 2. 各文件职责

### 2.1 `index.vue` —— 容器组件

**职责**：状态管理、数据请求、业务逻辑编排、弹窗 / 抽屉等交互态管理。

**禁止**：

- 在 `index.vue` 中内联编写表格列配置或搜索字段配置
- 在 `index.vue` 中直接渲染 `bk-table` / `bk-table-column`
- 在 `index.vue` 中直接渲染搜索表单字段

**必须**：

- 从 `children/data-list/column.ts` 导入列模型，通过 `getModel()` 获取列属性
- 从 `children/search/condition.ts` 导入条件模型，通过 `getModel()` 获取搜索字段
- 导入并使用 `children/data-list/data-list.vue` 和 `children/search/search.vue` 子组件
- 通过 props 向子组件传递数据，通过 events 接收子组件交互

**示例**：

```vue
<script setup lang="ts">
import { computed, ref, watch } from 'vue';
import { useRoute } from 'vue-router';
import { getModel } from '@/model/manager';
import { transformSimpleCondition } from '@/utils/search';
import useSearchQs from '@/hooks/use-search-qs';
import usePage from '@/hooks/use-page';
import { SearchCondition } from './children/search/condition';
import { TableColumn } from './children/data-list/column';
import Search from './children/search/search.vue';
import DataList from './children/data-list/data-list.vue';

const route = useRoute();
const { pagination, getPageParams } = usePage();

// 模型驱动：通过 getModel 获取列 / 字段属性
const conditionModel = getModel(SearchCondition);
const conditionProperties = computed(() => conditionModel.getProperties());

const columnModel = getModel(TableColumn);
const columnProperties = computed(() => columnModel.getProperties());

// 搜索条件 URL 持久化
const searchQs = useSearchQs({ key: 'filter', properties: conditionProperties });

const condition = ref<ISearchCondition>({});
const list = ref<IListItem[]>([]);
const loading = ref(false);

const fetchList = async () => {
  const { query } = route;
  condition.value = searchQs.get(query, {});
  pagination.current = Number(query.page) || 1;
  pagination.limit = Number(query.limit) || pagination.limit;
  loading.value = true;

  try {
    const filter = transformSimpleCondition(condition.value, conditionProperties.value);
    const page = getPageParams(pagination, {
      sort: (query.sort || 'created_at') as string,
      order: (query.order || 'DESC') as string,
    });
    const { list: data, count } = await store.getList({ filter, page });
    list.value = data || [];
    pagination.count = count || 0;
  } catch {
    list.value = [];
    pagination.count = 0;
  } finally {
    loading.value = false;
  }
};

watch(() => route.query, fetchList, { immediate: true });

// 搜索时重置分页 + 写入 URL
const handleSearch = (vals: ISearchCondition) => {
  pagination.current = 1;
  searchQs.set(vals);
};
const handleReset = () => {
  pagination.current = 1;
  searchQs.clear();
};
</script>

<template>
  <div class="xxx-list">
    <Search :fields="conditionProperties" :condition="condition" @search="handleSearch" @reset="handleReset" />
    <DataList
      v-bkloading="{ loading }"
      :columns="columnProperties"
      :list="list"
      :pagination="pagination"
      @view-details="handleViewDetails"
    />
  </div>
</template>
```

### 2.2 `children/data-list/data-list.vue` —— 纯展示表格组件

**职责**：渲染 `bk-table`，处理表格交互（分页、排序、选择），通过 emit 上报事件。

**禁止**：

- 在组件内发起数据请求
- 在组件内定义列配置
- 在组件内管理业务状态（如弹窗显隐、选中行的业务处理）

**必须**：

- 通过 props 接收 `columns`、`list`、`pagination`
- 通过 emit 上报用户交互（`view-details`、`select` 等）
- 使用 `usePage`、`useTableSelection`、`useTableSettings` 等 hook 处理表格通用能力
- 行操作按钮的禁用判断逻辑可放在组件内（纯 UI 状态判断），但业务动作通过 emit 上报
- 渲染 `display-value` 时通过 `v-bind="column?.props"` 将列定义中的 `props` 透传给值组件（如 `region` 类型需要 `vendor` 参数）

### 2.3 `children/data-list/column.ts` —— 列模型定义

**职责**：使用 `@Model` + `@Column` 装饰器声明表格列。`type` 参数驱动 `display-value` 组件的渲染，`meta` 配置外观与列行为。

**必须**：

- 使用 `@Model('xxx/table-column')` 声明模型
- 每个字段使用 `@Column(type, options)` 声明列属性
- `type` 参数从 `ModelPropertyType` 中选取（见下方类型系统章节），**决定 `display-value` 用哪个值组件渲染**
- 通过 `index` 控制列顺序
- 复杂展示（如状态标签）通过 `meta.display.appearance` 配置外观组件
- 仅服务端可见的列通过导出常量数组标注（如 `SERVICE_ONLY_COLUMNS`）

**示例**：

```typescript
import { Model, Column } from '@/decorator';
import { VendorEnum } from '@/common/constant';

export const SERVICE_ONLY_COLUMNS = ['op_product_name', 'bk_biz_id'];

@Model('gpu-demand/table-column')
export class TableColumn {
  @Column('string', { name: '需求ID', fixed: 'left', minWidth: 120, index: 0 })
  id: string;

  @Column('datetime', { name: '提单时间', sort: true, minWidth: 160, index: 5 })
  created_at: string;

  // type: 'region' → display-value 用 RegionValue 组件渲染
  // props: { vendor } → 透传给 RegionValue 组件，指定云厂商
  @Column('region', {
    name: '地域',
    minWidth: 100,
    index: 4,
    props: { vendor: VendorEnum.ZIYAN },
  })
  region: string;

  // type: 'enum' → display-value 用 EnumValue 组件渲染
  // meta.display.appearance: 'dynamic-status' → 套用 dynamic-status 外观
  @Column('enum', {
    name: '单据状态',
    option: STATUS_MAP,
    minWidth: 100,
    index: 7,
    meta: {
      display: {
        appearance: 'dynamic-status',
        appearanceProps: { statusObject: { success: ['DONE'], fail: ['REJECT'] } },
      },
    },
  })
  status: string;
}
```

### 2.4 `children/search/search.vue` —— 纯展示搜索表单组件

**职责**：渲染搜索表单字段，管理表单值，通过 emit 上报查询 / 重置。

**禁止**：

- 在组件内发起数据请求
- 在组件内定义搜索字段配置
- 在组件内处理 URL 持久化（由容器组件通过 `useSearchQs` 处理）

**必须**：

- 通过 props 接收 `fields`（字段属性数组）和 `condition`（当前条件值）
- 使用 `GridContainer` + `GridItemFormElement` + `GridItem` 布局
- 字段渲染使用动态组件 `<component :is="`hcm-search-${field.type}`" />`
- 通过 emit 上报 `search` 和 `reset` 事件
- `reset` 时恢复到初始 condition 值（非空表单）

**示例**：

```vue
<script setup lang="ts">
import { ref, watch } from 'vue';
import GridContainer from '@/components/layout/grid-container/grid-container.vue';
import GridItemFormElement from '@/components/layout/grid-container/grid-item-form-element.vue';
import GridItem from '@/components/layout/grid-container/grid-item.vue';

interface ISearchProps {
  fields: ModelPropertySearch[];
  condition: ISearchCondition;
}

const props = withDefaults(defineProps<ISearchProps>(), {});
const emit = defineEmits<{
  (e: 'search', condition: ISearchCondition): void;
  (e: 'reset'): void;
}>();

const formValues = ref<ISearchCondition>({});
let conditionInitValues: ISearchCondition;

const getSearchCompProps = (field: ModelPropertySearch) => {
  const compProps: Record<string, any> = { ...field.props };
  if (field.option !== undefined) compProps.option = field.option;
  if (field.list !== undefined) compProps.list = field.list;
  return compProps;
};

const handleSearch = () => emit('search', formValues.value);
const handleReset = () => {
  formValues.value = { ...conditionInitValues };
  emit('reset');
};

// 同步外部 condition → 内部 formValues，首次记录初始值用于重置
watch(
  () => props.condition,
  (condition) => {
    formValues.value = { ...condition };
    if (!conditionInitValues) {
      conditionInitValues = { ...formValues.value };
    }
  },
  { deep: true, immediate: true },
);
</script>

<template>
  <div class="search">
    <grid-container layout="vertical" :column="4" :gap="[16, 60]">
      <grid-item-form-element v-for="field in fields" :key="field.id" :label="field.name">
        <component :is="`hcm-search-${field.type}`" v-bind="getSearchCompProps(field)" v-model="formValues[field.id]" />
      </grid-item-form-element>
      <grid-item :span="4" class="row-action">
        <bk-button theme="primary" @click="handleSearch">查询</bk-button>
        <bk-button @click="handleReset">重置</bk-button>
      </grid-item>
    </grid-container>
  </div>
</template>
```

### 2.5 `children/search/condition.ts` —— 搜索条件模型定义

**职责**：使用 `@Model` + `@Column` 装饰器声明搜索字段。

**必须**：

- 使用 `@Model('xxx/search-condition')` 声明模型
- 每个字段使用 `@Column(type, options)` 声明搜索属性
- 通过 `index` 控制字段顺序
- 仅服务端可见的字段通过导出常量数组标注（如 `SERVICE_ONLY_FIELDS`）
- 字段的查询运算符通过 `meta.search.op` 配置
- 异步选项（如下拉列表数据）通过 `list` 属性异步函数获取

**示例**：

```typescript
import { Model, Column } from '@/decorator';
import { QueryRuleOPEnum } from '@/typings';

export const SERVICE_ONLY_FIELDS = ['op_product_id', 'bk_biz_id'];

@Model('xxx/search-condition')
export class SearchCondition {
  @Column('list', {
    name: '运营产品',
    list: async () => await store.getOpProductList(),
    format: (value) => toArray(value).map((val: string) => Number(val)),
    props: { idKey: 'op_product_id', displayKey: 'op_product_name' },
    index: 0,
  })
  op_product_id: number;

  @Column('string', {
    name: '需求ID',
    index: 2,
    meta: { search: { op: QueryRuleOPEnum.IN } },
    props: { collapseTags: true, pasteFn: (value) => /* ... */ },
  })
  id: string;

  @Column('datetime', {
    name: '提单时间',
    props: { type: 'daterange', format: 'yyyy-MM-dd' },
    index: 4,
  })
  created_at: string;
}
```

---

## 3. 类型系统与组件联动（核心机制）

`@Column(type, options)` 中的 `type` 是 **模型驱动渲染的核心**：同一个 `type` 同时决定**表格列展示**（经 `display-value`）和**搜索字段渲染**（经 `hcm-search-${type}`）。`meta` 则负责跨场景的差异化配置。

### 3.1 `ModelPropertyType` 类型清单

定义于 `front/src/model/typings.ts`，`type` 参数只能取以下值：

| type            | 表格展示组件（display-value） | 搜索组件（hcm-search-） | 典型用途                             |
| --------------- | ----------------------------- | ----------------------- | ------------------------------------ |
| `string`        | `StringValue`                 | `hcm-search-string`     | 文本、ID                             |
| `number`        | `NumberValue`                 | `hcm-search-number`     | 数量、计数                           |
| `datetime`      | `DatetimeValue`               | `hcm-search-datetime`   | 时间（支持 daterange）               |
| `enum`          | `EnumValue`                   | `hcm-search-enum`       | 枚举状态（配合 `option`）            |
| `list`          | （ArrayValue）                | `hcm-search-list`       | 异步下拉列表（配合 `list` 异步函数） |
| `array`         | `ArrayValue`                  | `hcm-search-array`      | 数组展示                             |
| `bool`          | `BoolValue`                   | `hcm-search-bool`       | 布尔值                               |
| `user`          | `UserValue`                   | `hcm-search-user`       | 用户选择器                           |
| `business`      | `BusinessValue`               | `hcm-search-business`   | 业务选择器                           |
| `account`       | `StringValue`                 | `hcm-search-account`    | 账号（搜索侧需 bizId/resourceType）  |
| `region`        | `RegionValue`                 | `hcm-search-region`     | 地域                                 |
| `cloud-area`    | `CloudAreaValue`              | `hcm-search-cloud-area` | 云区域                               |
| `cert`          | `CertValue`                   | `hcm-search-cert`       | 证书                                 |
| `ca`            | `CaValue`                     | `hcm-search-ca`         | CA                                   |
| `req-type`      | `ReqTypeValue`                | `hcm-search-req-type`   | 申请类型                             |
| `json`          | `JsonValue`                   | -                       | JSON 展示                            |
| `device-family` | -                             | -                       | 设备族                               |

> 完整映射见 `front/src/components/display-value/index.vue` 的 `valueComps`。

### 3.2 `meta` 跨场景配置

`ModelPropertyMeta`（`meta` 字段）统一管理同一字段在不同场景下的行为，**避免在多个地方重复定义**：

```typescript
type ModelPropertyMeta = {
  display?: PropertyDisplayConfig; // 表格列展示配置
  search?: PropertySearchConfig; // 搜索条件配置
  column?: PropertyColumnConfig; // 列行为配置（排序、固定、宽度等）
  form?: PropertyFormConfig; // 表单配置（校验、只读等）
};
```

#### 3.2.1 `meta.display` —— 展示外观

通过 `appearance` 指定外观组件（位于 `front/src/components/display-value/appearance/`），`appearanceProps` 传参：

```typescript
@Column('enum', {
  name: '单据状态',
  option: STATUS_MAP,
  meta: {
    display: {
      appearance: 'dynamic-status',          // 外观类型
      appearanceProps: {                      // 外观组件 props
        statusObject: {
          success: ['DONE'],
          fail: ['REJECT', 'REJECT_ALL'],
          wait: ['INIT'],
          ing: ['PENDING'],
          stop: ['TERMINATE'],
        },
      },
    },
  },
})
status: string;
```

可选的 `AppearanceType`：`status` | `link` | `wxwork-link` | `tag` | `cvm-status` | `clb-status` | `dynamic-status` | `business-assign-tag` | `link-button` | `link-popover`。

还支持 `format`（值格式化）和 `render`（完全自定义渲染）。

#### 3.2.2 `meta.search` —— 搜索运算符与格式化

```typescript
@Column('string', {
  name: '需求ID',
  meta: {
    search: {
      op: QueryRuleOPEnum.IN,           // 查询运算符
      filterRules: (value) => ({...}),  // 自定义过滤规则
      format: (value) => any,            // 值格式化
      enableEmpty: true,                 // 允许空值查询
    },
  },
})
id: string;
```

#### 3.2.3 `option` / `list` —— 枚举与异步选项

- `option: Record<string, any>` —— 静态枚举映射（用于 `enum` 类型）
- `list: async () => [...]` —— 异步获取选项列表（用于 `list` 类型）

```typescript
// 静态枚举
@Column('enum', { name: '状态', option: STATUS_MAP })
status: string;

// 异步列表
@Column('list', {
  name: '运营产品',
  list: async () => await store.getOpProductList(),
  props: { idKey: 'op_product_id', displayKey: 'op_product_name' },
})
op_product_id: number;
```

#### 3.2.4 `props` —— 透传给值组件的额外参数

`props?: Record<string, any>` 是 `PropertyColumnConfig` 的顶层字段，用于向 `display-value` 渲染的值组件传递额外参数。配置后被 `data-list.vue` 通过 `v-bind="column?.props"` 展开到 `display-value`，再经其 `useAttrs()` 透传到具体值组件。

典型场景：

```typescript
// region 类型 → RegionValue 组件需要 vendor 参数区分云厂商
@Column('region', {
  name: '地域',
  props: { vendor: VendorEnum.ZIYAN },
})
region: string;

// list 类型 → hcm-search-list 组件需要 idKey/displayKey
@Column('list', {
  name: '运营产品',
  list: async () => await store.getOpProductList(),
  props: { idKey: 'op_product_id', displayKey: 'op_product_name' },
})
op_product_id: number;
```

**与 `meta.display.appearanceProps` 的区别**：`props` 透传给值组件本身（如 `RegionValue`），而 `appearanceProps` 透传给外观组件（如 `DynamicStatus`）。两者互不干扰。

### 3.3 模型驱动渲染流程

```
@Model + @Column 定义
        │
        ▼
getModel(ModelClass).getProperties()
        │
        ├──► ModelPropertyColumn[] ──► DataList ──► display-value ──► 按 type 选值组件 + 按 appearance 套外观
        │
        └──► ModelPropertySearch[] ──► Search ──► hcm-search-${type} ──► 按 type 选搜索组件
```

**关键点**：`type` 一致则表格与搜索的值类型一致，`meta` 负责场景差异化。不要在 `data-list.vue` 或 `search.vue` 里硬编码类型判断，一切由模型驱动。

---

## 4. 接口规范（list, count 模式）

### 4.1 核心原则

凡包含「搜索栏 + 数据列表」的页面，**必须**采用标准的 `list, count` 接口模式。接口接收 `{ filter, page }` 参数，返回 `{ list, count }` 结构。禁止在接口中直接传递扁平参数，也禁止返回非标准结构。

**⚠️ 重要提醒**：

- `list, count` 模式**只适用于支持分页的接口**
- 如果接口不支持分页，或接口参数特殊（如裁撤总览列表 `POST /api/v1/woa/dissolve/table/list` 直接传参而非 filter 形式），则需要灵活对待
- 在实现前，务必确认后端接口的请求参数格式和返回数据结构
- **不确定时，必须追问用户或查看 API 文档，不要盲目套用标准模式**

### 4.2 接口请求参数格式

```typescript
{
  filter?: Record<string, any>;  // 搜索条件（由 transformSimpleCondition 转换）
  page: {
    start: number;               // 分页起始位置
    limit: number;               // 每页条数
    sort?: string;               // 排序字段
    order?: 'ASC' | 'DESC';     // 排序方向
  };
}
```

### 4.3 接口返回格式

```typescript
{
  list: T[];        // 数据列表
  count: number;    // 总条数
}
```

### 4.4 Store 接口方法标准写法

```typescript
const getList = async (params: { filter?: Record<string, any>; page: Record<string, any> }) => {
  loading.value = true;
  try {
    const res: IQueryResData<{ list: IItemType[]; count: number }> = await http.post('/api/v1/xxx/list', params);
    return res?.data;
  } catch (error) {
    console.error(error);
    return Promise.reject(error);
  } finally {
    loading.value = false;
  }
};
```

### 4.5 容器组件中的数据请求

```typescript
const fetchList = async (searchCondition?: Record<string, any>) => {
  const cond = searchCondition ?? condition.value;
  loading.value = true;
  try {
    const sort = (route.query.sort as string) || 'created_at';
    const order = (route.query.order as string) || 'DESC';

    const { list, count } = await store.getList({
      filter: transformSimpleCondition(cond, conditionProperties.value),
      page: getPageParams(pagination, { sort, order }),
    });

    list.value = list || [];
    pagination.count = count || 0;
  } catch {
    list.value = [];
    pagination.count = 0;
  } finally {
    loading.value = false;
  }
};
```

### 4.6 关键工具函数

- **`transformSimpleCondition(condition, properties)`**：将搜索条件转换为后端可识别的 filter 格式
- **`getPageParams(pagination, { sort, order })`**：根据分页状态生成 page 参数

### 4.7 禁止的反模式

```typescript
// ❌ 禁止：直接传递扁平参数（标准分页接口）
const params = {
  bk_biz_ids: cond.bk_biz_ids,
  project_ids: cond.project_ids,
  page: { start: 0, limit: 20 },
};
const res = await store.getList(params);

// ❌ 禁止：返回非标准结构
// 后端返回 { details: [], count: 0 } 或 { items: [], total: 0 }
// 前端需要统一映射为 { list: [], count: number }
```

**正确**：使用 `filter` 包裹搜索条件，返回 `{ list, count }` 结构。

**例外情况**：

- 如果后端接口确实不支持标准 `filter` 格式（如裁撤总览列表），可以在 Store 方法中手动映射参数
- 如果后端返回结构不是 `{ list, count }`，可以在 Store 方法中映射为标准结构
- **但容器组件中的 `fetchList` 必须保持统一的调用方式**，不直接处理参数映射

---

## 5. 多资源类型场景：工厂模式

当同一个列表页需要支持多种资源类型（如 `task/` 模块支持多种云厂商资源），列定义和搜索条件需采用**工厂模式**，按「通用 + 资源特有」拆分：

```
xxx/children/
├── data-list/
│   ├── data-list.vue            # 根据 props.resource 调用工厂获取列
│   ├── column-factory.ts        # 工厂：根据资源类型返回对应列属性数组
│   ├── column-common.ts         # 通用列定义（所有资源共享）
│   ├── column-tcloud.ts         # 腾讯云特有列定义
│   └── column-xxx.ts            # 其他资源特有列定义
└── search/
    ├── search.vue               # 根据 props.resource 调用工厂获取字段
    ├── condition-factory.ts     # 工厂：根据资源类型返回对应搜索字段
    ├── condition-common.ts      # 通用搜索条件
    ├── condition-tcloud.ts      # 腾讯云特有搜索条件
    └── condition-xxx.ts         # 其他资源特有搜索条件
```

**设计要点**：

- `column-common.ts` 定义所有资源通用的列（如 ID、状态、提单时间）
- `column-tcloud.ts` 定义某资源特有的列（如腾讯云特有字段）
- 工厂负责按资源类型**合并**通用列与特有列，返回最终的 `ModelPropertyColumn[]`
- `data-list.vue` 通过 `props.resource` 调用工厂，**不在组件内判断资源类型**

**工厂示例**：

```typescript
// column-factory.ts
import columnCommon from './column-common';
import columnTcloud from './column-tcloud';

export default function columnFactory() {
  const { getCommonColumns } = columnCommon();
  const { getTcloudColumns } = columnTcloud();

  const getColumnsByResource = (resource: string) => {
    const common = getCommonColumns();
    if (resource === 'tcloud') return [...common, ...getTcloudColumns()];
    return common;
  };

  return { getColumns: getColumnsByResource };
}
```

```typescript
// data-list.vue 中的使用
import columnFactory from './column-factory';
const { getColumns } = columnFactory();
const columns = getColumns(props.resource); // 模型驱动，不在组件内判断类型
```

---

## 6. 通信契约

### 6.1 容器 → 子组件（Props Down）

| 子组件     | Props                            | 说明                                                         |
| ---------- | -------------------------------- | ------------------------------------------------------------ |
| `Search`   | `fields: ModelPropertySearch[]`  | 搜索字段属性数组（来自 `getModel().getProperties()` 或工厂） |
| `Search`   | `condition: ISearchCondition`    | 当前条件值（来自 URL 解析）                                  |
| `DataList` | `columns: ModelPropertyColumn[]` | 列属性数组（来自 `getModel().getProperties()` 或工厂）       |
| `DataList` | `list: T[]`                      | 列表数据                                                     |
| `DataList` | `pagination: PaginationType`     | 分页信息                                                     |
| `DataList` | `resource?: string`              | 资源类型（工厂模式场景）                                     |

### 6.2 子组件 → 容器（Events Up）

| 子组件     | Event                | 说明                                  |
| ---------- | -------------------- | ------------------------------------- |
| `Search`   | `search(condition)`  | 用户点击查询                          |
| `Search`   | `reset`              | 用户点击重置                          |
| `DataList` | `view-details(row)`  | 查看详情                              |
| `DataList` | `select(selections)` | 行选择变化                            |
| `DataList` | 其他业务事件         | 如 `adjust`、`reject`、`terminate` 等 |

---

## 7. 禁止的反模式

### 7.1 内联列定义

```vue
<!-- ❌ 禁止：在 index.vue 或 data-list.vue 中内联写列配置 -->
<bk-table-column prop="id" label="ID" />
<bk-table-column prop="name" label="名称" />
```

```typescript
// ❌ 禁止：用普通数组定义列
const columns = [
  { prop: 'id', label: 'ID' },
  { prop: 'name', label: '名称' },
];
```

**正确**：使用 `@Model` + `@Column` 装饰器在独立的 `column.ts` 中定义。

### 7.2 在展示组件内硬编码类型判断

```typescript
// ❌ 禁止：在 data-list.vue 或 search.vue 中写 type 分支
if (column.type === 'enum') {
  return <EnumValue />;
} else if (column.type === 'datetime') {
  return <DatetimeValue />;
}
```

**正确**：由模型 `type` 驱动，`display-value` 和 `hcm-search-${type}` 自动选择组件。组件内只做 `<display-value :property="column" :value="row[column.id]" />`。

### 7.3 搜索组件内发请求

```typescript
// ❌ 禁止：在 search.vue 中直接调用 API
const handleSearch = async () => {
  const data = await api.search(formValues.value);
  emit('search', data);
};
```

**正确**：`search.vue` 只 emit 条件值，由 `index.vue` 负责请求。

### 7.4 容器组件内渲染表格 / 表单字段

```vue
<!-- ❌ 禁止：在 index.vue 中直接写 bk-table 和搜索字段 -->
<template>
  <div>
    <bk-form>
      <bk-form-item label="名称"><bk-input v-model="name" /></bk-form-item>
    </bk-form>
    <bk-table :data="list">
      <bk-table-column prop="id" label="ID" />
    </bk-table>
  </div>
</template>
```

**正确**：使用 `<Search>` 和 `<DataList>` 子组件。

### 7.5 不建 children 目录，平铺所有文件

```
# ❌ 禁止：平铺结构
xxx/list/
├── index.vue
├── search.vue
├── data-list.vue
├── column.ts
└── condition.ts
```

**正确**：使用 `children/data-list/` 和 `children/search/` 分组。

### 7.6 工厂模式场景在组件内判断资源类型

```typescript
// ❌ 禁止：在 data-list.vue 中写资源类型分支
if (props.resource === 'tcloud') {
  columns = tcloudColumns;
} else {
  columns = commonColumns;
}
```

**正确**：调用工厂 `getColumns(props.resource)`，分支逻辑收敛在工厂内。

---

## 8. 检查清单

新建或重构列表页时，逐项确认：

- [ ] 目录结构符合 `index.vue` + `children/data-list/` + `children/search/`
- [ ] `index.vue` 只做容器编排，不内联列 / 字段定义，不使用 `router.push` 直接跳转
- [ ] `index.vue` 中 `handleSearch` 重置 `pagination.current = 1` 再调用 `searchQs.set()`
- [ ] `data-list.vue` 是纯展示组件，不发请求、不管理业务态、不硬编码类型判断
- [ ] `search.vue` 是纯展示组件，不发请求、不处理 URL 持久化、不硬编码类型判断
- [ ] `search.vue` 使用 `conditionInitValues` 模式实现重置回初始值
- [ ] `search.vue` 中 `getSearchCompProps` 传递 `option` 和 `list` 给搜索组件
- [ ] `column.ts` 使用 `@Model` + `@Column` 装饰器定义列，`type` 取自 `ModelPropertyType`
- [ ] `column.ts` 导出专用字段（如 `exportFormatter`）用于表格列格式化
- [ ] `condition.ts` 使用 `@Model` + `@Column` 装饰器定义搜索条件，`type` 与列保持一致
- [ ] 列 / 字段通过 `getModel().getProperties()` 获取，非手动构造数组
- [ ] 表格单元格通过 `<display-value :property="column" :value="row[column.id]" v-bind="column?.props" />` 渲染
- [ ] 搜索字段通过 `<component :is="`hcm-search-${field.type}`" />` 渲染
- [ ] 复杂外观通过 `meta.display.appearance` 配置，不在组件内自定义
- [ ] 搜索运算符通过 `meta.search.op` 配置，值格式化通过 `meta.search.format`
- [ ] 子组件通过 props 接收数据，通过 emit 上报交互（不通过 `$refs` 直接调方法）
- [ ] 多资源类型场景使用工厂模式（`column-factory.ts` / `condition-factory.ts`），不在组件内判断资源类型
- [ ] 分页接口走 `filter + page → { list, count }` 标准模式（特殊接口可例外，但需在 store 层映射）
- [ ] 文件命名遵循 kebab-case
- [ ] 路由跳转使用 `routerAction` 而非 `router.push` / `router.replace`

---

## 9. 模块路由注册模式（route-config.ts → router/module 聚合）

### 9.1 原则

模块路由由 `views/<模块>/route-config.ts` 定义，**聚合到 `router/module/<view>.ts` 中**（如 `service.ts`、`business.ts`），而非在 `views/index.ts` 中逐个拼接。这与 ticketRoutes 的注册方式完全一致，保持路由聚合层的单一入口。

### 9.2 注册步骤

**第 1 步**：在 `router/module/<view>.ts` 中导入 route-config：

```typescript
// router/module/service.ts
import ticketRoutes from '@/views/ticket/route-config';
import dissolveRoutes from '@/views/server-dissolve/route-config';
```

**第 2 步**：spread 到对应视图的 children 中：

```typescript
const serviceMenus: RouteRecordRaw[] = [
  {
    path: '/service',
    children: [...ticketRoutes, ...dissolveRoutes],
  },
];
```

**第 3 步**：`views/index.ts` 只需直接导出，无需额外的 filter/map/拼接：

```typescript
// views/index.ts
import service from '@/router/module/service';

export const serviceViews = service; // 干净，不用拼接
```

### 9.3 禁止

```typescript
// ❌ 禁止：在 views/index.ts 中 filter 旧路由再拼接新路由
const serviceWithoutDissolve = service.map((routeGroup) => {
  if (routeGroup.path === '/service' && routeGroup.children) {
    return {
      ...routeGroup,
      children: routeGroup.children.filter((child) => child.path !== '/service/dissolve'),
    };
  }
  return routeGroup;
});
export const serviceViews = [...serviceWithoutDissolve, ...dissolveRoutes];
```

**原因**：`views/index.ts` 应保持简单的聚合导出，不应包含路由去重/过滤逻辑。如果存在重复路由，应当追到源头清理（如删除旧硬编码路由块，替换为 route-config import）。

### 9.4 根级菜单模块的 route meta 配置

当模块是**根级菜单**（一级菜单，如机房裁撤的 `/service/dissolve`），需在 route meta 中设置正确的属性：

```typescript
// server-dissolve/route-config.ts 实际示例
meta: {
  ...new Meta({
    owner: MENU_SERVICE,
    title: '机房裁撤',
    activeKey: MENU_SERVICE_DISSOLVE,
    layout: {
      breadcrumbs: { show: true, back: false },
    },
  }),
}
```

**关键属性说明**：

- **`owner`**：所属父级视图（`MENU_SERVICE` / `MENU_RESOURCE` / `MENU_BUSINESS`）
- **`activeKey`**：当前路由对应的菜单高亮 key，确保页签高亮正确
- **`layout.breadcrumbs.show: true`**：启用 breadcrumb（Teleport 插槽可用）
- **`layout.breadcrumbs.back: false`**：根级菜单没有上一级，隐藏返回按钮

> **⚠️ 注意**：`notMenu` 已废弃，仅在被废弃的 `views/home/index.tsx` 中消费。新架构下菜单由 `menu-service.ts` 独立管理，完全与路由解耦，不需要也不应该设置 `notMenu`。详见 `@deprecated-patterns` 规则。

---

## 10. 全局 breadcrumb + Teleport 导航栏模式

### 10.1 背景

项目中存在一个全局 breadcrumb 组件（`views/home/breadcrumb.tsx`），渲染为 `.navigation-breadcrumb` 容器。它根据路由 meta 的 `title` 和 `layout.breadcrumbs.show` 控制显示，并提供 3 个 Teleport 目标插槽：

| DOM ID             | CSS 类              | 用途                                      |
| ------------------ | ------------------- | ----------------------------------------- |
| `#breadcrumbLeft`  | —                   | 标题文字左侧扩展区域（如 VendorSelector） |
| `#breadcrumbHead`  | `.breadcrumb-head`  | 标题右侧信息区（如状态、日期选择器）      |
| `#breadcrumbExtra` | `.breadcrumb-extra` | 最右侧操作按钮区（`margin-left: auto`）   |

### 10.2 使用模式

**场景一：简单页面（标题 + 操作按钮）**—— 如 `server-dissolve/index.vue`：

1. `route-config.ts` 中启用 breadcrumb：

```typescript
meta: {
  ...new Meta({
    title: '机房裁撤',
    layout: { breadcrumbs: { show: true } },
  }),
}
```

2. 页面组件中移除自定义标题栏，用 Teleport 注入操作按钮：

```vue
<template>
  <div class="xxx-page">
    <!-- 将操作按钮注入到全局 breadcrumb 最右侧 -->
    <Teleport defer to="#breadcrumbExtra">
      <bk-button @click="handleSync">同步</bk-button>
      <bk-button theme="primary" @click="handleConfig">配置</bk-button>
    </Teleport>

    <!-- 页面主体内容 -->
    <bk-tab v-model:active="tab" type="unborder-card">...</bk-tab>
  </div>
</template>
```

3. 页面不再需要自定义 header 样式，只需内容区 padding：

```scss
.xxx-page {
  padding: 0 24px;
}
```

**场景二：带扩展信息的页面**—— 如 `resource-plan/gpu/detail/index.vue`：

```vue
<template>
  <Teleport defer to="#breadcrumbHead">
    <div class="breadcrumb-head-info">
      <span class="breadcrumb-separator">|</span>
      <span class="breadcrumb-order-id">{{ orderId }}</span>
      <!-- 状态图标 -->
    </div>
  </Teleport>
  <Teleport defer to="#breadcrumbExtra">
    <bk-button>重新导入数据</bk-button>
  </Teleport>
</template>
```

**场景三：标题左侧注入**—— 如 `cloud-account-manage/index.vue`：

```vue
<template>
  <Teleport defer to="#breadcrumbLeft">
    <VendorSelector style="margin-left: 12px" v-model="vendor" />
  </Teleport>
</template>
```

### 10.3 原则

- **标题由路由 meta 驱动**：`title` 声明在 `route-config.ts` 的 `Meta` 中，不在页面组件中渲染自定义标题
- **操作按钮用 Teleport**：不在页面内写 header 容器 + buttons，而是 Teleport 注入到 breadcrumb 对应插槽
- **Teleport 使用 `defer`**：等待 DOM 挂载完成再移动，避免初始渲染找不到目标
- **页面只需 content padding**：全局 breadcrumb 本身有固定高度和定位，页面组件只负责内容区布局

### 10.4 禁止

```vue
<!-- ❌ 禁止：在页面内自定义 header 含标题 + 按钮 -->
<div class="xxx-header">
  <span class="xxx-title">机房裁撤</span>
  <div class="xxx-actions">
    <bk-button>同步</bk-button>
    <bk-button theme="primary">裁撤配置</bk-button>
  </div>
</div>
```

---

## 11. 多 Tab 模块目录结构

### 11.1 适用场景

当一个页面模块包含**多个独立 Tab**，每个 Tab 都有各自的搜索栏 + 数据列表（如机房裁撤的「裁撤总览」和「裁撤明细」），需要将 Tab 状态共享、联动跳转逻辑封装在 hooks 中，各 Tab 作为独立子模块组织。

### 11.2 目录结构

```
views/<模块>/
├── route-config.ts              # 模块路由配置
├── index.vue                    # Tab 容器：Tab 切换 + Teleport 按钮 + 弹窗挂载
├── <tab-a>/                     # 第一个 Tab 子模块（如 summary 总览）
│   ├── index.vue                # Tab 容器：getModel + props down + 数据请求
│   └── children/
│       ├── search/
│       │   ├── search.vue       # 纯展示搜索组件
│       │   └── condition.ts     # 搜索条件 @Model + @Column
│       └── data-list/
│           ├── data-list.vue    # 纯展示表格组件
│           └── column.ts        # 表格列 @Model + @Column
├── <tab-b>/                     # 第二个 Tab 子模块（如 device-list 设备列表）
│   ├── index.vue
│   └── children/
│       ├── search/
│       │   ├── search.vue
│       │   └── condition.ts
│       └── data-list/
│           ├── data-list.vue
│           └── column.ts
├── hooks/                       # Tab 共享状态 composable（如 use-dissolve-tabs.ts）
│   └── use-xxx-tabs.ts
├── components/                  # Tab 共享子组件（弹窗、选择器等）
│   ├── sync-dialog/
│   └── config-dialog/
└── typings.ts                   # [可选] 模块内闭环类型定义
```

### 11.3 参考实现

`views/server-dissolve/`（机房裁撤模块）：

```
server-dissolve/
├── route-config.ts              # 路由定义 + breadcrumb 启用
├── index.vue                    # Tab 容器（Teleport 注入按钮 + bk-tab + provide 共享数据）
├── overview/                    # 裁撤总览
│   ├── index.vue                # 容器：getModel + props down + 数据请求
│   └── children/
│       ├── search/{search.vue, condition.ts}
│       └── data-list/{data-list.vue, column.ts}
├── detail/                      # 裁撤明细
│   ├── index.vue
│   └── children/
│       ├── search/{search.vue, condition.ts}
│       └── data-list/{data-list.vue, column.ts}
├── components/                  # Tab 共享子组件（弹窗、选择器等）
│   ├── sync-dialog/
│   ├── config-dialog/
│   └── org-tree-selector/
└── store/                       # [可选] 模块内闭环 store
```

### 11.4 index.vue（Tab 容器）职责

- **Tab 状态管理**：通过 `computed` get/set 双向绑定 `route.query.tab`，实现 URL 驱动的 Tab 切换
- **provide 共享数据**：将各 Tab 共用的数据（如配置列表、选项列表）通过 `provide` 注入给子组件
- **Teleport 操作按钮**：将页级操作按钮注入到 breadcrumb 的 `#breadcrumbExtra`
- **弹窗挂载**：SyncDialog、ConfigDialog 等级别弹窗挂载在 index.vue 中
- **数据刷新**：弹窗操作成功后重新调用数据获取函数

```vue
<script setup lang="ts">
import { computed, ref, provide, onMounted } from 'vue';
import { useRoute, type LocationQueryRaw } from 'vue-router';
import routerAction from '@/router/utils/action';
import TabA from './children/tab-a/index.vue';
import TabB from './children/tab-b/index.vue';

const route = useRoute();

// URL 驱动的 Tab 状态
const activeTab = computed<string>({
  get: () => (route.query.tab as string) || 'tab-a',
  set: (value: string) => {
    const query = { ...route.query, tab: value } as LocationQueryRaw;
    delete query.sort;
    delete query.order;
    routerAction.redirect({ query }, { replace: true });
  },
});

// provide 共享数据给子 Tab
const sharedData = ref([]);
provide('sharedData', sharedData);

onMounted(() => fetchSharedData());
</script>

<template>
  <div class="xxx-page">
    <Teleport defer to="#breadcrumbExtra">
      <bk-button @click="handleAction">操作按钮</bk-button>
    </Teleport>

    <bk-tab v-model:active="activeTab" type="unborder-card">
      <bk-tab-panel v-for="tab in tabs" :key="tab.name" render-directive="if"
        :label="tab.label" :name="tab.name">
        <component :is="tab.component" />
      </bk-tab-panel>
    </bk-tab>
  </div>
</template>
```

### 11.5 Tab 子模块（总览/明细）index.vue 职责

每个 Tab 子模块的 `index.vue` 遵循标准的列表页容器模式（同 §2.1），额外增加：

- **inject 父级共享数据**：`const sharedData = inject<Ref<DataType>>('sharedData')`
- **作为独立的列表页容器**：拥有自己的 `getModel` / `useSearchQs` / `usePage` / `fetchList`
- **URL 参数独立**：每个 Tab 的搜索条件通过 `filter` query 参数独立持久化

### 11.6 跨 Tab 联动跳转

跨 Tab 跳转通过 URL 参数传递搜索条件，而非组件内 carryOver 模式：

```typescript
// 从总览 Tab 点击某行，跳转到明细 Tab 并携带筛选条件
const handleRowClick = (row: Record<string, any>) => {
  const filter = searchQs.build({ bk_biz_ids: [row.bk_biz_id] });
  routerAction.redirect({
    query: { ...route.query, filter, tab: 'detail' },
  }, { replace: true });
};

// 明细 Tab 的 index.vue 中自动从 URL 解析 filter
watch(() => route.query, (query) => {
  condition.value = searchQs.get(query, {});
  fetchList();
}, { immediate: true });
```

### 11.7 命名约定

- Tab 子目录名使用**简洁语义化英文**：常见如 `overview`（总览）、`detail`（明细）、`summary`（概要）
- 跨 Tab 共享的数据获取放在模块根级 `index.vue` 中通过 `provide/inject` 注入
- 跨 Tab 共享的子组件（弹窗、选择器）放在模块根级 `components/`
- 模块级 store（如 `store/dissolve/quota.ts`）负责 API 调用，不放在 views 目录内
