---
description: >-
  HCM：TAPD 需求澄清回写时必须保留原 description。在执行 tapd-story-clarification、
  澄清回写、stories_update(description)、规范化需求文档回写 TAPD 时必读。
alwaysApply: false
---

# TAPD 澄清回写 — 保留原需求描述（HCM）

HCM 工作流会跑 `tapd-story-clarification`，该 skill 用全量 `stories_update`/`bugs_update` 改写 `description`。编排层必须把「保留原文」当成可检查闸口，不能假设子 agent 自觉遵守。**不修改澄清 skill 文件本身。**

## 闸口（每张单据、每次带 `description` 的 update 之前）

1. **快照**：澄清开始时（或回写前立刻）`stories_get` / `bugs_get` 取得当前 `description`，在会话内完整保留。原文为空则跳过本闸，仍可回写规范化文档。
2. **拟写校验**：准备写入的 Markdown 必须同时满足：
   - 存在「原需求描述」章节（`## 原需求描述` 或同级等价标题）；
   - 该章节非空，且覆盖快照实质内容（至少含快照中一段连续 ≥40 字片段；原文更短则整段原文须出现）；
   - **禁止**用摘要 / 改写 /「见上文」替代原文粘贴。
3. **失败禁止写**：任一条件不满足 → **禁止**调用带 `description` 的 update；先补齐再写。不得以「模板已生成」「赶时间」「原文太长」为由跳过。
4. **并行派发**：多单 Task 跑澄清时，prompt 须显式带上本闸口，并要求子 agent 回传时声明「已含原需求描述且已对照快照」。

## 范围

只约束**改写 description** 的 update；纯 `status` / `owner` 等字段的 `tapdSync` 追平不受影响。
