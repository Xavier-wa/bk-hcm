# Design：<feature 标题>

> 编写前已通过 `blueking-figma-dev` §0–5.5（经 `wf-design-figma-intake` 包装早停）或用户 PNG 识别稿面。冲突以稿面 + 用户确认为准。**本阶段不出业务代码。**

## 0. 本子需求范围

| 在本迭代内 | 不在本迭代内 |
|------------|--------------|
| | |

## 1. 设计稿引用

| 状态 | Figma | Node |
|------|-------|------|
| | | |

## 2. 信息架构 / 布局

## 3. 关键交互

## 3.x 关键图标语义（Coding 必读）

> **语义列**由 design 主流程填写（只写视觉语义，不写项目 CSS 类名）。  
> **项目候选列**由项目级图标 skill（如 HCM `wf-design-icon-intake`）在 design.post 回填；无项目 skill 时该列可空，coding 按「语义 → Grep 项目资源 → 组件库」自选并记入 `coding.md`。

| 稿面位置 | 语义描述 | Node | 项目候选（类名 / 组件，可空） |
|----------|----------|------|------------------------------|
| | | | |

## 3.y 组件候选（Coding 必读）

> 通识列来自 `blueking-figma-dev` §4–5（主流程填写）。只记候选与是否已读组件 skill reference；**不贴完整 props API**。  
> **项目三列**（复用层级 / 落码入口 / 项目路径）由项目级 skill（如 HCM `wf-design-comp-intake`）在 design.post 回填；无项目 skill 时三列可空，coding 按相邻代码 → 组件库兜底并记入 `coding.md`。

| 稿面区域/语义 | 组件候选 | 体系（bkui / magic / 扩展包） | 文档确认（已读 reference / 未读） | 复用层级（page/comp/adjacent/none，可空） | 落码入口（skill 名或「无」，可空） | 项目路径/说明（可空） |
|---------------|----------|------------------------------|----------------------------------|------------------------------------------|-----------------------------------|----------------------|
| | | | | | | |

待确认（map + skill 索引未命中、需手写等）：

- 

## 4. 状态流转

## 5. 异常 / 边界态

| 场景 | 展示 |
|------|------|
| | |

## 6. 与 PRD 差异（如有）

| 项 | PRD | 稿面/确认结论 |
|----|-----|---------------|
| | | |

## 7. 与 PRD 验收映射

| PRD | Design |
|-----|--------|
| | |

## 8. 实现边界（design 纪要）

> 对应 `blueking-figma-dev` §5.5。写入此处 **不等于** 授权改业务代码；落码在 coding 阶段。

| 项 | 内容 |
|----|------|
| 目标目录意向 | |
| 组件/手写边界 | |
| 明确不做 | |
| 数据（真接口 / mock） | |
