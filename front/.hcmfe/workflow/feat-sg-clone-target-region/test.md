# feat-sg-clone-target-region 测试清单

> 工作流: `feat-sg-clone-target-region`
> 关联文档: PRD / Design / API / Coding
> TAPD: story 1069995598136749088（安全组-克隆支持选择目标地域）

## 验证范围

- 功能边界: 克隆安全组弹窗新增「目标地域」下拉（默认源地域、可选该 vendor 可用地域全集）、提交透传 `target_region`、成功提示携带目标地域名、负责人/备份负责人默认回填源值可修改。
- 不在范围: 批量多地域克隆、跨业务复制、克隆后自动跳转、后端接口行为（后端已支持 `target_region`）、菜单入口。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`（敏感信息用占位符，不写真实账号）
- 数据准备:
  - 测试业务下已有一个腾讯云（TCLOUD）安全组（克隆入口仅对 TCLOUD 开放）
  - 该业务至少覆盖 2 个可用地域的 tcloud 地域数据（`POST /api/v1/cloud/vendors/tcloud/regions/list` 正常返回）
  - 异常分支：可用浏览器开发者工具将地域列表接口或克隆接口拦截为 500 mock

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 弹窗默认态 | 已登录，某 TCLOUD 安全组在列表 | 点击该安全组「克隆」 | 弹窗出现「目标地域」下拉，默认值为源安全组地域；选项为 tcloud 可用地域全集 | 关闭弹窗 |
| P0-02 | 默认同地域克隆（回归） | P0-01 后 | 不改地域，填写/保留名称，确认提交 | 克隆成功，提示「已克隆至 <源地域名>！」，弹窗关闭且停留当前列表；新安全组出现在源地域 | 删除克隆出的安全组 |
| P0-03 | 跨地域克隆 | P0-01 后，业务含多个地域 | 目标地域改为其他地域，确认提交 | 克隆成功，提示「已克隆至 <目标地域名>！」，停留当前列表；后端收到 `target_region`（网络面板核对） | 删除克隆出的安全组 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 负责人回填与修改 | 打开克隆弹窗 | 查看「负责人/备份负责人」 | 默认为源安全组的值；修改后提交，新安全组负责人为修改后值 |
| P1-02 | 克隆接口失败 | mock 克隆接口 500 | 填写表单确认提交 | 复用现有错误提示框架展示后端 message，弹窗不关闭、列表无新增 |
| P1-03 | 地域列表失败降级 | mock 地域列表接口 500 | 打开克隆弹窗 | 下拉为空、无报错阻断；直接提交时默认源地域 ID 仍可走通（不空值） |
| P1-04 | 目标地域非空校验 | 构造源数据 region 为空的脏数据（或 DevTools 清空表单值） | 打开弹窗清空目标地域后提交 | 表单红字提示「目标地域不能为空」，不发请求 |

### P2 - UI 细节（按需）

- [ ] 目标地域下拉支持输入过滤（filterable）
- [ ] 地域列表加载中下拉展示 loading 态
- [ ] 弹窗关闭后重新克隆另一个安全组：目标地域默认值正确切换为新源安全组地域（v-if 重建，无残留）
- [ ] 名称超 60 字符校验仍生效（回归）

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | 待 QA 验证 | |
| P0-02 | 待 QA 验证 | |
| P0-03 | 待 QA 验证 | |
| P1-01 | 待 QA 验证 | |
| P1-02 | 待 QA 验证 | |
| P1-03 | 待 QA 验证 | |
| P1-04 | 待 QA 验证 | |

- 执行日期: 2026-08-28
- 总体结论: 待 QA 验证（本清单为自动化流程产出的手测清单，验证结论统一标「待 QA 验证」）
- 后续行动: 等待 QA 按 P0/P1 用例复测；P0 全 PASS 后可合入
