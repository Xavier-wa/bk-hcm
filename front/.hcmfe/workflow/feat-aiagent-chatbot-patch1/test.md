# feat-aiagent-chatbot-patch1 测试清单

> 工作流: `feat-aiagent-chatbot-patch1`（lite）
> 关联文档: `coding.md`
> 本轮覆盖 TAPD:
> - `1069995598136381200` 切换窗口时闪现「停止生成」按钮
> - `1069995598136401202` 首页大卡片快速点击重复请求（防抖）
> - `1069995598136402240` 主机申请浮窗入口缺少权限控制
>
> 历史批次（默认提示词 / 停止提示）已在上一轮 test→done 验收通过，本轮不重复列为必测。

## 验证范围

- 功能边界:
  - 切回对话页 / 拉历史时，无续传则不闪「停止生成」；有断点续传时仍可停止。
  - 首页大卡片连点只触发一次发送。
  - 无 `biz_agent_assistant` 时不展示主机申请页 AI 浮窗入口。
- 不在范围: 后端 cancel；多标签页状态同步；小卡片/输入框防重；权限系统本身改造。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，执行时确认；含全页 /chatbot 与业务页浮窗两处入口>`
- 测试账号:
  - 有 `biz_agent_assistant`：`<test_account_with_perm>`
  - 无 `biz_agent_assistant`：`<test_account_without_perm>`
- 数据准备:
  - P0-01：准备一条已有历史消息的会话（非空会话）。
  - P0-02（可选）：若环境支持 history 断点续传，准备一个生成中被中断、可续传的会话。
  - P0-05：使用无助手权限账号登录（或临时去掉权限）访问主机申请页。

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 切回对话页不闪停止 | 打开有历史的会话，再离开到其它业务页 | 再切回对话页，观察加载过程与加载完成后的输入区 | 全程不出现「停止生成」/发送位停止态 | 无 |
| P0-02 | 会话切换拉历史不闪停止 | 至少 2 个有历史的会话 | 在会话列表间切换 | 加载历史时输入区保持发送态；无续传时不出现停止按钮 | 无 |
| P0-03 | 大卡片连点只发一次 | 全页 `/chatbot` 首页空态 | 快速连点同一张大卡片 | 只产生一次发送 / 一条用户消息；卡片呈禁用态 | 新建会话 |
| P0-04 | 大卡片交替连点 | 首页空态 | 快速交替点两张大卡片 | 仅第一次点击生效 | 新建会话 |
| P0-05 | 无权限隐藏浮窗 | 无 `biz_agent_assistant` 账号 | 打开主机申请页；尝试 Cmd/Ctrl+I | 无悬浮球/面板；快捷键不唤起；业务「首页」菜单亦不可见 | 切回有权限账号 |
| P0-06 | 有权限浮窗正常 | 有 `biz_agent_assistant` 账号 | 打开主机申请页，打开浮窗并发送一句 | 浮窗可用，对话正常 | 无 |

### P1 - 边界/回归（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 正常生成中停止仍可用 | 全页发送问题进入生成中 | 观察停止按钮；点停止 | 生成中显示停止；停止后「已停止生成」提示仍正常（回归任务二） |
| P1-02 | 快速连续切换会话 | 多个有历史会话 | 快速连续切换 | 旧 history 被 abort，无串会话、无残留停止态 |
| P1-03 | 大卡片单击正常 | 首页空态 | 单击一张大卡片 | 正常直发，行为与改前一致 |
| P1-04 | 空会话/首页无停止按钮 | 首页空态或空会话 | 观察输入区 | 无停止按钮 |

### P2 - UI 细节（按需）

- [ ] 大卡片禁用态：`cursor: not-allowed`、半透明，连点无指针事件
- [ ] 浮窗有权限时深链 `sessionCode` 唤起仍正常

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤 / 截图 / 日志）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 自测 |
| P0-02 | PASS | 自测 |
| P0-03 | PASS | 自测 |
| P0-04 | PASS | 自测 |
| P0-05 | PASS | 自测 |
| P0-06 | PASS | 自测 |
| P1-01 | PASS | 自测 |
| P1-02 | PASS | 自测 |
| P1-03 | PASS | 自测 |
| P1-04 | PASS | 自测 |

- 执行日期: 2026-07-23
- 总体结论: PASS
- 后续行动: 推进 done / TAPD 收口
