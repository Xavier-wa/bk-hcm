# feat-cvm-apply-subnet-available-ip 测试清单

> 工作流: `feat-cvm-apply-subnet-available-ip`
> 关联文档: PRD / Design / API / Coding

## 验证范围

- 功能边界: 自研云资源申领界面与未生产需求调整界面（共用 `CvmSubnetSelector`）的子网下拉，选项内联展示"可用IP"数量，并在子网表单项下方注明口径。
- 不在范围: 扩容/变配等其他界面；IP 预警、不足时拦截提交、自动推荐网段；公有云/非自研云子网。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认>`
- 测试账号: `<test_account>`
- 数据准备:
  - 自研云（`tcloud_ziyan`）业务账号，具备"资源申领"与"未生产需求调整"入口权限。
  - 后端子网接口 `POST /api/v1/woa/config/findmany/config/cvm/subnet` 已上线（`available_ip_count` 字段返回 CRP `leftIpNum`）。
  - 准备至少一条 `available_ip_count > 0` 的子网，一条 `available_ip_count = 0` 的子网。
  - 异常分支：临时用 mock/抓包工具拦截该接口，让 `available_ip_count` 缺失或整体失败，验证兜底展示。

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 申领界面子网下拉展示可用IP | 进入资源申领，选好 VPC | 展开子网下拉 | 每个选项显示 `subnetId | subnetName | 可用IP: X`，X 与后端返回一致 | 关闭下拉 |
| P0-02 | 修改界面子网下拉展示可用IP | 进入未生产需求调整（修改申请） | 展开子网下拉 | 同 P0-01，选项内联显示可用IP | 关闭下拉 |
| P0-03 | 选择子网并提交不受影响 | 选中带可用IP的子网 | 提交申领/修改 | 原有选择与提交流程正常，可用IP仅展示不影响提交 | — |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 可用IP为0仍展示且可选 | 存在 `available_ip_count = 0` 的子网 | 展开下拉并选中该子网 | 显示 `可用IP: 0`，选项可选中、可提交，不被禁用 |
| P1-02 | 可用IP为null显示问号 | mock 拦截接口使 `available_ip_count` 为 `null` | 展开下拉 | 显示 `可用IP: ?`，不显示 undefined/null/0，清单照常返回 |
| P1-03 | 口径说明展示 | 展开网络信息面板 | 查看子网表单项下方 | 显示「可用IP 为云厂商未分配可用 IP 数」提示 |

### P2 - UI 细节（按需）

- [ ] 子网下拉选项超长时（subnetId+名称+可用IP）无溢出，显示完整或合理截断
- [ ] 口径说明样式与现有提示一致（小字号弱化，不干扰主流程）
- [ ] 仅自研云场景展示可用IP（公有云/非自研云不出现该列）

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 申领界面子网下拉内联显示可用IP，数值与后端一致 |
| P0-02 | PASS | 修改界面子网下拉内联显示可用IP |
| P0-03 | PASS | 选择带可用IP的子网提交正常，不受展示影响 |
| P1-01 | PASS | 可用IP=0 显示 `可用IP: 0`，仍可选可提交 |
| P1-02 | 待重测 | 2026-08-19 契约变更：null 由显示 `0` 改为显示 `?`，需按新用例重测 |
| P1-03 | PASS | 口径说明「可用IP 为云厂商未分配可用 IP 数」展示正常 |

- 执行日期: 2026-08-17（P1-02 于 2026-08-19 因契约变更标记待重测）
- 总体结论: PASS（待 P1-02 重测确认）
- 后续行动: 合入
