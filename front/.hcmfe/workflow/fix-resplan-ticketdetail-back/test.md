# fix-resplan-ticketdetail-back 测试清单

> 工作流: `fix-resplan-ticketdetail-back`
> 关联文档: PRD / Coding
> TAPD: https://<TAPD_HOST>/tapd_fe/69995598/story/detail/1069995598137606214
> 负责人: 开发自测 / QA

## 验证范围

- 功能边界: 资源预测申请单详情页内返回箭头。调整提交后进入详情应能回到单据列表；从单据列表进入详情后返回仍回列表。
- 不在范围: 迁新面包屑；返回调整页或预测资源列表；浏览器后退键；普通申请单详情。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`
- 数据准备:
  - 业务视角：有可调整的资源预测，提交后能进入申请单详情
  - 同一业务下单据列表能打开该详情（资源预测 tab）
  - 工作台视角：能打开同类资源预测申请单详情

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 调整提交后详情返回单据列表 | 业务视角进入资源预测调整页，提交成功并进入申请单详情 | 点击页内详情头返回箭头 | 进入单据列表，停留在资源预测 tab、当前业务 | 无 |
| P0-02 | 列表进入详情后仍回列表 | 业务视角单据列表（资源预测）打开同一张详情 | 点击页内详情头返回箭头 | 回到单据列表（资源预测 tab），不空白、不报错 | 无 |
| P0-03 | 工作台详情返回工作台列表 | 工作台视角打开同类资源预测申请单详情 | 点击页内详情头返回箭头 | 回到工作台单据列表（资源预测） | 无 |

### P1 - 回归（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 提交后返回不去调整页 | 同 P0-01，已在提交后的详情页 | 点击页内返回箭头 | 落地页是单据列表，不是调整页，也不是预测资源列表 |
| P1-02 | 详情仍用旧页内头 | 提交后详情或列表进入的详情 | 查看页头 | 仍是页内左箭头标题，不是新全局面包屑 |

### P2 - UI 细节（按需）

- [ ] 返回箭头可点，无响应空白
- [ ] 返回后业务 ID 与进入详情前一致

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 开发自测确认 |
| P0-02 | PASS | 开发自测确认 |
| P0-03 | PASS | 开发自测确认 |
| P1-01 | PASS | 开发自测确认 |
| P1-02 | PASS | 开发自测确认 |

- 执行人: 开发自测
- 执行日期: 2026-08-27
- 总体结论: PASS —— 用户确认手测通过，推进至 done
- 后续行动: 合入
