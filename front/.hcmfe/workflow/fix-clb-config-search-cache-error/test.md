# fix-clb-config-search-cache-error 测试清单

> 工作流: `fix-clb-config-search-cache-error`
> 关联文档: Coding

## 验证范围

- 功能边界: 负载均衡「配置检索」页 RS tab 的搜索切换、IP 分组勾选、批量调整权重/解绑弹窗数据一致性
- 不在范围: 监听器 tab、URL 规则 tab、批量导出功能、后端接口逻辑

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认>`
- 测试账号: `<test_account>`
- 数据准备:
  - 确保测试业务下已有至少 2 个不同的 RS IP 且各有若干关联 RS
  - 在配置检索页选择一个云账号（有负载均衡数据的地域）

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 搜索切换不报错 | 已进入配置检索页 RS tab | 搜索 RS IP-A → 查看结果 → 清空 RS IP 输入框 → 搜索 RS IP-B | 页面正常展示 IP-B 的搜索结果，浏览器控制台无 `Cannot read properties of null/undefined` 报错 | 无需回滚 |
| P0-02 | 切换搜索后 IP 分组全选 | P0-01 完成后 | 展开 IP-B 的分组面板，点击 IP 左侧复选框 | 该 IP 下所有 RS 行均被勾选，顶部显示"已选 N 个RS"且数量 = 该 IP 下实际 RS 数 | 点击「一键清空」 |
| P0-03 | 切换搜索后批量调整权重弹窗数据正确 | P0-02 完成后 | 点击「批量调整RS权重」按钮 | 弹窗中展示的 IP 为 IP-B 的数据（非 IP-A），RS 数量与勾选一致 | 关闭弹窗 |
| P0-04 | 切换搜索后批量解绑弹窗数据正确 | P0-02 完成后 | 点击「批量解绑RS」按钮 | 弹窗中展示的 IP 为 IP-B 的数据（非 IP-A），RS 数量与勾选一致 | 关闭弹窗 |
| P0-05 | 单次搜索无回归 | 已进入配置检索页 RS tab | 搜索 RS IP-A，展开面板，勾选 IP，查看弹窗 | 行为与改动前一致，无异常 | 关闭弹窗 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 快速连续搜索 | 已进入配置检索页 RS tab | 在 2 秒内连续搜索 3 个不同 RS IP（不等前一个结果返回） | 页面仅展示最后一次搜索的结果，数据不闪烁不跳变，控制台无报错 |
| P1-02 | 批量操作弹窗中删除单个 IP | 已勾选 2 个 IP | 打开批量调整权重弹窗，点击某个 IP 行的删除按钮 | 该 IP 从弹窗列表中移除，剩余 IP 数量正确，弹窗可正常操作 |
| P1-03 | 搜索切换后分页正常 | 搜索结果 > 10 条 | 切换搜索后翻页 | 分页正常，每页数据正确，全选状态不受翻页影响 |
| P1-04 | 监听器 tab 不受影响 | 已进入配置检索页 | 在 RS tab 搜索切换后，切换到「监听器」tab，再切回 RS tab | 监听器 tab 数据正常展示，切回 RS tab 后状态正确（collapse 重置、选中状态已清空） |

### P2 - UI 细节（按需）

- [ ] 搜索无结果时空态文案正确（"没有数据"）
- [ ] 批量导出按钮在勾选 RS 后可正常使用
- [ ] 已选数量超过 5000 时显示警告提示，不显示批量操作按钮

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 搜索切换无报错 |
| P0-02 | PASS | IP 分组全选正确 |
| P0-03 | PASS | 批量调整权重弹窗数据一致 |
| P0-04 | PASS | 批量解绑弹窗数据一致 |
| P0-05 | PASS | 单次搜索无回归 |
| P1-01 | PASS | 快速连续搜索展示最后一次结果 |
| P1-02 | PASS | 弹窗删除单 IP 正常 |
| P1-03 | PASS | 分页切换正常 |
| P1-04 | PASS | 监听器 tab 不受影响 |
| P2 | PASS | 空态/导出/5000 上限均正常 |

- 执行日期: 2026-07-22
- 总体结论: PASS
- 后续行动: 直接合入
