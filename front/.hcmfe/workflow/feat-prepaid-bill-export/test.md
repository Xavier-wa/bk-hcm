# feat-prepaid-bill-export 测试清单

> 工作流: `feat-prepaid-bill-export`
> 关联文档: PRD / Design / API / Coding
> TAPD: 预付费账单查询结果导出 `1069995598137371363`

## 验证范围

- 功能边界: 预付费列表工具条「导出」按当前筛选下载 xlsx（22 列、核算不遮蔽、0 条禁用、无条数上限）。
- 不在范围: 勾选导出、CSV、详情/分摊导出、独立导出接口、改造列表筛选本身。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`
- 数据准备:
  - 当前筛选命中 ≥ 1 条（含至少 1 条未定账）
  - 可构造命中 0 条的筛选（如不可能存在的 GPU 型号）
  - 仅授权部分二级账号的账号（核对文件不含未授权行）
  - 大数据量（>500 或 >1000）若环境有则测分页拼装；没有则 P2 跳过
  - 导出失败：导出过程中拦截 `prepaid_items/list` 返回 500，或断网

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 按当前筛选导出 | 筛选后列表有数据 | 点「导出」，确认弹窗后下载 | 下载 xlsx；行数等于当前命中条数（分页 count）；不是库内未筛选全量 | 删除本地下载文件 |
| P0-02 | 导出 22 列口径一致 | 已下载文件 | 对照列表列 | 列名、顺序与列表 22 列一致；订单月份为 `YYYY-MM`（来自 `order_at`）；账号/产品为名称；核算状态/单据状态/云厂商为中文 | 删除文件 |
| P0-03 | 未定账核算不遮蔽 | 筛选结果含未定账行 | 打开导出文件对照列表 | 累计核算金额、核算状态与列表真实值一致，不是 `--` | 删除文件 |
| P0-04 | 0 条禁用 | 筛选命中 0 条 | 查看导出按钮 | 按钮不可用，不下载空表 | 清空筛选 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 导出失败 | 有命中数据；导出中让 list 失败 | 点导出 | 失败提示；不留下半成品文件；列表本身不被破坏 |
| P1-02 | 行级隔离 | 仅部分二级账号权限 | 导出当前结果 | 文件中不含未授权账号行 |
| P1-03 | 进行中可终止 | 命中较多、导出未完成 | 点导出后切换筛选/离开或取消（若组件提供） | 进行中按钮态；终止后不再继续写文件 |

### P2 - UI 细节（按需）

- [ ] 按钮文案为「导出」，带下载图标，描边样式
- [ ] 文件名以「预付费账单」为前缀（组件会加时间戳）
- [ ] 无 1000 条上限：命中 >1000 仍可导出（环境无数据则跳过）
- [ ] 超过 500 条时按 500 分页拼装，最终行数仍等于 count
- [ ] 列头漏斗条件不进入导出请求
- [ ] 无 CSV 选项

## 验证结论

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 |  |  |
| P0-02 |  |  |
| P0-03 |  |  |
| P0-04 |  |  |
| P1-01 |  |  |
| P1-02 |  |  |
| P1-03 |  |  |

- 执行日期:
- 总体结论: PASS / FAIL
- 后续行动:
