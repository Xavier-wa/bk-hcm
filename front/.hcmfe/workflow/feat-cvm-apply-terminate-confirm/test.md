# feat-cvm-apply-terminate-confirm 测试清单

> 工作流: `feat-cvm-apply-terminate-confirm`
> 关联文档: Coding（lite 模式，无 prd/design/api 产物）

## 验证范围

- 功能边界: 两个页面的"终止"操作前增加 InfoBox 二次确认弹窗（待交付设备数 + 灰底提示文案）
  - 【资源管理-单据管理-主机申请】`ziyanScr/hostApplication/components/application-list`
  - 【服务请求-主机申领-申请单据】`ticket/children/host-apply/applications`
- 不在范围: 其他页面（host-recycle、sub-ticket-list、host-recycle/bill-detail 等）的终止操作；后端终止接口；权限体系；审计日志

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认>`
- 测试账号: `<test_account>`
- 数据准备:
  - 至少 1 条存在"待交付设备"（pending_num > 0）的主机申请单据，状态为可终止
  - 至少 1 条 pending_num = 0 的单据（验证文案边界）

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 资源管理页终止弹确认框 | 主机申请列表有可终止单据 | 点击操作列"终止" | 弹出 InfoBox，标题"确认需要终止该单据？"，含"待交付设备：N 台"+ 灰底提示文案，按钮为"终止""取消" | 点取消关闭 |
| P0-02 | 资源管理页确认终止 | P0-01 弹窗已弹出 | 点击"终止"按钮 | 弹窗关闭，调用 stopOrder，成功后 Toast"终止成功"，列表刷新 | 重新申请恢复 |
| P0-03 | 资源管理页取消终止 | P0-01 弹窗已弹出 | 点击"取消"或关闭按钮 | 弹窗关闭，不调用 stopOrder，列表无变化 | 无需回滚 |
| P0-04 | 服务请求页终止弹确认框 | 主机申领申请单据列表有可终止单据 | 点击操作列"终止" | 同 P0-01 | 点取消关闭 |
| P0-05 | 服务请求页确认终止 | P0-04 弹窗已弹出 | 点击"终止"按钮 | 同 P0-02 | 重新申请恢复 |
| P0-06 | 服务请求页取消终止 | P0-04 弹窗已弹出 | 点击"取消"或关闭按钮 | 同 P0-03 | 无需回滚 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 待交付数为 0 的单据 | 存在 pending_num=0 的可终止单据 | 点击"终止" | 弹窗显示"待交付设备：0 台"，灰底提示文案不变，确认后可正常终止 |
| P1-02 | 终止接口失败 | mock stopOrder 返回错误 | 弹窗点"终止" | Toast 失败，列表无变化（沿用现有错误处理） |
| P1-03 | quickClose 不触发提交 | P0-01 弹窗已弹出 | 点击遮罩层或按 ESC | 弹窗不关闭（quickClose:false），不提交终止 |

### P2 - UI 细节（按需）

- [ ] InfoBox 标题为"确认需要终止该单据？"且居中对齐
- [ ] "待交付设备：N 台"文案与单据实际待交付数一致（对照列表"待交付数"列）
- [ ] 灰底提示框背景色为 #f5f7f9，文案"终止后将停止交付剩余设备，且该操作不可恢复，请谨慎操作！"
- [ ] "终止"按钮为 danger 主题（红色）
- [ ] 终止按钮 disabled 条件沿用原 opBtnDisabled，不可终止时按钮置灰
- [ ] 两个页面的 InfoBox 表现一致

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 资源管理页终止弹确认框正常 |
| P0-02 | PASS | 确认终止调用 stopOrder 成功 |
| P0-03 | PASS | 取消不提交，列表无变化 |
| P0-04 | PASS | 服务请求页终止弹确认框正常 |
| P0-05 | PASS | 确认终止调用 stopOrder 成功 |
| P0-06 | PASS | 取消不提交，列表无变化 |
| P1-01 | PASS | pending_num=0 文案正确 |
| P1-02 | PASS | 接口失败沿用现有错误处理 |
| P1-03 | PASS | quickClose:false 遮罩不关闭 |

- 执行日期: 2026-08-11
- 总体结论: PASS
- 后续行动: 推进 done，准备提交 MR
