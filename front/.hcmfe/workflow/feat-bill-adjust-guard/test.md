# feat-bill-adjust-guard 测试清单

> 工作流: `feat-bill-adjust-guard`
> 关联文档: PRD / Design / API / Coding
> TAPD: 账单调整列表状态与守卫 `1069995598137371364`

## 验证范围

- 功能边界: 现网账单调整列表新增推送/定账两列 Tag，以及编辑/删除/勾选/批量操作守卫。
- 不在范围: 预付费管理菜单/列表/详情/导出；手工新增表单；推送状态人工置位；`source`/`source_id` 列与跳转。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`（需有账单调整页操作权限）
- 数据准备:
  - 预付费来源行（`source=prepaid`，任意推送/定账）
  - 人工来源 + 未确认 + 未推送 + 未定账（可勾选、可编辑删除）
  - 人工来源 + 已确认 + 已推送 + 未定账（可编辑删除，不可勾选批量确认）
  - 人工来源 + 推送中
  - 人工来源 + 已定账
  - 推送失败且带 `push_fail_reason` 的行
  - 无新字段的存量行（缺 `source`，已确认）——无则 P0-07 用接口改响应或跳过
  - 写失败：编辑/删除时拦截更新接口返回错误

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 两列 Tag 展示 | 调整列表有数据 | 打开账单调整页并横向滚动 | 「调账状态」后可见「推送状态」「定账状态」；推送四态：未推送/推送中/已推送/失败，无超时；定账：未定账/已定账；空/未知为 `--` | 无需 |
| P0-02 | 失败原因 tooltip | 存在 `push_status=failed` 且有失败原因 | 悬停失败 Tag | 看到失败原因；非失败态无该说明；无独立失败原因列 | 无需 |
| P0-03 | 预付费来源锁定 | `source=prepaid` 行 | 看编辑/删除/勾选 | 编辑、删除禁用；不可勾选；禁用说明为「OFS 预付费调账不可人工修改。」 | 无需 |
| P0-04 | 人工已确认可改 | 人工 + 已确认 + 非推送中 + 未定账 | 点编辑保存（可只改备注） | 允许进入编辑；保存成功后列表刷新（以后端为准，常见未确认+未推送）；不乐观改本地状态 | 按后端结果核对 |
| P0-05 | 推送中/已定账禁用 | 人工推送中行、人工已定账行各一条 | 看编辑/删除 | 均禁用；推送中 tooltip 含「推送中」；已定账 tooltip 含「已定账」 | 无需 |
| P0-06 | 批量勾选守卫 | 同时有未确认人工、已确认人工、预付费行 | 尝试勾选并点批量确认/删除 | 仅未确认且非 prepaid、非 pushing、非 settled 可勾选；已确认人工不能作为批量确认目标 | 取消勾选 |
| P0-07 | 缺来源降级 | 无 `source` 的存量已确认行 | 看编辑/删除 | 不可编辑/删除（不误放开）；未确认存量行仍可操作 | 无需 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 写失败不乐观更新 | 一条本可编辑的人工行 | 编辑保存时让接口失败 | 展示接口错误；列表该行状态/推送不本地改写 |
| P1-02 | 无 source_id 跳转 | 预付费来源行 | 查看各列 | 无来源单据 ID 列，无跳转预付费详情 |

### P2 - UI 细节（按需）

- [ ] 选择列固定左侧；调账状态、推送状态、定账状态、操作固定右侧
- [ ] 币种、备注不固定
- [ ] 增加两列后，当月列表查询无明显额外无关接口
- [ ] 搜索、导出、新建、导入入口行为与改前一致
- [ ] 推送/定账 Tag 主题：推送中 info、已推送 success、失败 danger、已定账 success

## 验证结论

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 |  |  |
| P0-02 |  |  |
| P0-03 |  |  |
| P0-04 |  |  |
| P0-05 |  |  |
| P0-06 |  |  |
| P0-07 |  |  |
| P1-01 |  |  |
| P1-02 |  |  |

- 执行日期:
- 总体结论: PASS / FAIL
- 后续行动:
