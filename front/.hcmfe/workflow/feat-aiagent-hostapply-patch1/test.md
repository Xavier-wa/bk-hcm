# feat-aiagent-hostapply-patch1 测试清单

> 工作流: `feat-aiagent-hostapply-patch1`
> 关联文档: PRD / Design / API / Coding

## 验证范围

- 功能边界: host-apply chatbot 推荐卡片 / 预提单表 / 提交表 / 调整配置弹窗中，「机型」展示由纯编码 enrichment 为 `{编码} ({机型族}, {n}核{m}G)`
- 不在范围: agent SSE 协议字段、非 chatbot 主机申领表单页、顶部推荐摘要文案、后端接口改造

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`（敏感信息用占位符，不写真实账号）
- 数据准备:
  - 准备可触发主机申领推荐的对话，使 Agent 返回含有效 `device_type`（ZIYAN 配置中存在）的方案
  - 准备一条配置中不存在的机型编码（或拦截 `getOneDevicetype` 返回空）用于降级场景
  - 异常分支：DevTools / mock 让机型查询失败或慢返回，验证首屏不阻断

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 推荐卡片机型富文本 | 推荐方案含配置中存在的机型编码 | 打开 Template A（推荐/预览）卡片，查看「机型」行 | 展示形如 `S5.LARGE8 (标准型, 4核8G)`；数值与配置一致 | 无 |
| P0-02 | 预提单/提交表机型列 | 同一会话进入 Template B 或 D，表内含 ≥1 行机型 | 查看表格「机型」列 | 各行按同一富文本规则展示；不同机型各自正确 | 无 |
| P0-03 | 配置缺失降级 | 方案机型编码在 ZIYAN 配置中不存在 | 查看 A/B/D 机型展示 | 仅显示原始编码，不出现空白/报错；其余字段与操作正常 | 无 |
| P0-04 | 查询延迟不阻断 | mock 机型查询慢返回或失败 | 打开推荐卡片并尝试继续操作 | 首屏至少显示编码；不白屏；卡片操作可用；成功后可原地刷新为富文本（若最终有 meta） | 取消 mock |
| P0-05 | floating / fullpage 一致 | 同一推荐方案 | 分别在 floating 助手与全屏 chatbot 查看机型 | 展示格式一致 | 无 |

### P1 - 异常分支 / 次要路径（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 调整配置下拉富文本 | 打开 Template C 调整配置弹窗 | 展开机型下拉，查看选项文案；选中一项后看表单值 | 选项 label 含机型族/CPU/内存；选中值仍为机型编码 |
| P1-02 | 同机型只请求一次 | B/D 表多行相同 `device_type` | 打开表格，观察网络请求 | 相同机型不重复打单条查询（或命中会话缓存） |
| P1-03 | 部分规格缺失 | meta 仅有部分字段（如仅有 cpu_core） | 查看机型行 | 有则拼、缺则省略，不出现 `undefined` / 占位符 |

### P2 - UI 细节（按需）

- [ ] 机型列过长时表格 `show-overflow-tooltip` 可完整查看
- [ ] 机型行/列无额外错位、样式与其他规格行一致
- [ ] 会话内切换方案后，机型展示随当前 `device_type` 更新

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 用户确认推进 done |
| P0-02 | PASS | 用户确认推进 done |
| P0-03 | PASS | 用户确认推进 done |
| P0-04 | PASS | 用户确认推进 done |
| P0-05 | PASS | 用户确认推进 done |
| P1-01 | PASS | 用户确认推进 done |
| P1-02 | PASS | 用户确认推进 done |
| P1-03 | PASS | 用户确认推进 done |

- 执行日期: 2026-07-13
- 总体结论: PASS
- 后续行动: 推进工作流至 done；合入按分支发布流程执行
