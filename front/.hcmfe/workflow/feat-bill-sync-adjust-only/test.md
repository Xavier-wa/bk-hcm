# feat-bill-sync-adjust-only 测试清单

> 工作流: `feat-bill-sync-adjust-only`
> 关联文档: PRD / Design / API / Coding

## 验证范围

- 功能边界: 账单同步弹窗新增"仅同步调账数据"模式，含同步范围选择、调账金额汇总展示、API sync_mode 参数传递；操作记录表格新增"同步范围"列
- 不在范围: 操作记录页面（需后端接口支持，不在本次前端范围内）；汇率与同步日期在调账模式下的后端屏蔽逻辑

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`（敏感信息用占位符，不写真实账号）
- 数据准备:
  - 存在有调账数据的业务（bk_biz_id），用于验证"仅同步调账数据"模式
  - 存在无调账数据的业务，用于验证前置条件的禁用逻辑
  - 调账数据字段：cny_cost、usd_cost 有值，adjustment_item_count > 0

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | "全量同步"默认选中 | 已登录，进入账单概览页，选中业务 | 点击"账单同步"按钮打开弹窗 | "全量同步" radio 默认选中，显示汇率、同步日期、供应商预览 | 关闭弹窗 |
| P0-02 | 切换到"仅同步调账数据" | 已打开账单同步弹窗 | 选择"仅同步调账数据" | 1. 汇率选择器隐藏 2. 同步日期选择器隐藏 3. 调账数据区域出现，显示人民币金额、美金金额、合计金额、业务数量 | 切换回"全量同步" |
| P0-03 | 全量同步提交 | 选中"全量同步"，填写汇率日期 | 点击"同步"按钮 | 1. API 请求携带 sync_mode="full" 2. 请求成功，弹窗关闭 | — |
| P0-04 | 仅同步调账数据提交 | 选中"仅同步调账数据" | 点击"同步"按钮 | 1. API 请求携带 sync_mode="adjustment_only" 2. 请求成功，弹窗关闭 | — |
| P0-05 | 无调账数据时"仅同步调账数据"禁用 | 选中无调账数据的业务 | 打开账单同步弹窗 | "仅同步调账数据" radio 禁用（disable），hover 提示"当前无调账金额" | — |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 全量同步校验 | 选中"全量同步"，不选汇率 | 点击"同步"按钮 | 汇率红字校验提示，不发送请求 |
| P1-02 | 切换同步模式后清空选择 | 全量同步下选好汇率 → 切到调账 → 切回全量 | 查看汇率选择器和日期选择器 | 之前的选择被清空/重置 |
| P1-03 | 接口失败 | mock sync_records 接口 500 | 任一模式触发同步 | Toast 提示失败，弹窗不关闭，不重复请求 |
| P1-04 | 调账金额接口失败 | mock adjustment_items/sum 接口 500 | 切换到"仅同步调账数据" | 调账数据区域显示 0 或空，不妨碍提交 |
| P1-05 | 超长账户名折行 | 供应商名超过 30 字符 | 查看供应商显示区域 | 文本不撑破布局 |

### P2 - UI 细节（按需）

- [ ] 弹窗竖向滚动条在内容溢出时出现，不会遮挡同步范围选择区
- [ ] "全量同步"/"仅同步调账数据" radio 间距正常，hover 和 selected 状态一致
- [ ] 调账金额橙色（#f59500）字体显示正常
- [ ] 响应式：弹窗在 1280 / 1440 / 1920 三种宽度下布局正常，无横向滚动
- [ ] 同步按钮 loading 态正常，防止重复提交

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | <PASS / FAIL / Skipped> | |
| P0-02 | <PASS / FAIL / Skipped> | |
| P0-03 | <PASS / FAIL / Skipped> | |
| P0-04 | <PASS / FAIL / Skipped> | |
| P0-05 | <PASS / FAIL / Skipped> | |
| P1-01 | <PASS / FAIL / Skipped> | |
| P1-02 | <PASS / FAIL / Skipped> | |
| P1-03 | <PASS / FAIL / Skipped> | |
| P1-04 | <PASS / FAIL / Skipped> | |
| P1-05 | <PASS / FAIL / Skipped> | |

- 执行日期: <YYYY-MM-DD>
- 总体结论: PASS / FAIL（FAIL 时需在 Issue / TAPD 单据中跟进）
- 后续行动: <例如：回退到 coding 阶段修复 P0-01 / 直接合入 / 等待 QA 复测>
