# fix-prepaid-bill-search-params 测试清单

> 工作流: `fix-prepaid-bill-search-params`
> 关联文档: Coding

## 验证范围

- 功能边界: 预付费账单列表「GPU 型号」「产品名称」多关键词包含搜索的请求 filter；列表与导出共用同一份 `transformSimpleCondition`。
- 不在范围: 后端接口、搜索 UI 改版、其它筛选字段语义、操作记录/CLB 页面回归（仅 helper 兼容）。

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`
- 数据准备:
  - 预付费列表有可匹配的产品名称、GPU 型号数据（至少两条不同关键词能各自命中）
  - 浏览器开发者工具可查看 list / 导出请求的 `filter`

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 产品名称多关键词包含任意匹配 | 列表有数据 | 产品名称输入两个关键词后查询 | 请求发出且不报 `cs.value` 须为 string；`product_name` 为外层 `and` 下一组 `or`，两条 `cs` 的 value 均为单个字符串 | 清空筛选 |
| P0-02 | GPU 型号多关键词包含任意匹配 | 列表有数据 | GPU 型号输入两个关键词后查询 | 同 P0-01，字段为 `gpu_type` | 清空筛选 |
| P0-03 | 单关键词 | 列表有数据 | 只填一个产品名称关键词查询 | 一条 `cs`，value 为字符串，不是数组 | 清空筛选 |
| P0-04 | 导出与列表同一 filter | 已按 P0-01 筛选出结果 | 点击导出 | 导出请求的 filter 与列表一致，不报错 | — |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 未填关键词 | 打开列表 | 不填产品名称/GPU 型号，只查其它条件或空查 | 这两字段不出现在 `filter.rules` |
| P1-02 | 两字段同时填 | 列表有数据 | 产品名称与 GPU 型号都填多关键词 | 外层 `and` 下两组 `or`；其它筛选仍在 |
| P1-03 | 其它筛选不变 | 列表有数据 | 使用时间 / 订单月份 / 账号等照常筛选 | 时间仍为嵌套 `and`；账号等仍为原算子 |

### P2 - UI 细节（按需）

- [x] 搜索区控件仍为 tag 多值，无新交互
- [x] 使用时间、订单月份非法区间仍被入口拦截

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 执行人确认测试通过 |
| P0-02 | PASS | 执行人确认测试通过 |
| P0-03 | PASS | 执行人确认测试通过 |
| P0-04 | PASS | 执行人确认测试通过 |
| P1-01 | PASS | 执行人确认测试通过 |
| P1-02 | PASS | 执行人确认测试通过 |
| P1-03 | PASS | 执行人确认测试通过 |

- 执行日期: 2026-08-27
- 总体结论: PASS
- 后续行动: 推进 done，TAPD 追到已完成
