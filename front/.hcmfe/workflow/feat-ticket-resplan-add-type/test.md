# feat-ticket-resplan-add-type 测试清单

> 工作流: `feat-ticket-resplan-add-type`
> 关联文档: PRD（docs/reqs/预算申报类型展示.md）/ Coding（coding.md）
> 需求: 单据管理 → 资源预测，新增类型 `budget_declare`（预算申报）前端展示适配与筛选项

## 验证范围

- 功能边界: 资源预测单据列表「类型」列展示"预算申报"、「类型」筛选项支持"预算申报"、「CPU核数(已审批数)」列对预算申报按增量绿色展示。
- 不在范围: 子单 `sub_ticket_type` 维度；后端 meta / 列表接口对 `budget_declare` 的返回（后端范围）；预算申报单据的详情/新建/编辑等其他交互；原有类型的回归以"无变化"为准，不整模块回归。

## 测试环境

- 前端入口: `<资源预测单据列表的测试环境地址，执行时由执行人确认；勿写入仓库>`（路径：单据管理 → 资源预测 Tab；业务视角与资源/工作台视角均可）
- 测试账号: `<test_account>`
- 数据准备:
  - **依赖后端**：需测试环境存在类型为 `budget_declare` 的资源预测单据，且列表接口返回 `ticket_type=budget_declare` 与 `ticket_type_name="预算申报"`；meta 接口 `/api/v1/woa/metas/ticket_types/list` 返回该类型。
  - 若后端尚未就绪，可用浏览器拦截 mock：
    - 列表接口 `.../plans/resources/tickets/list` 响应中构造一条 `ticket_type:"budget_declare"`、`ticket_type_name:"预算申报"`，并设置 `audited_updated_info.cvm.cpu_core` 大于 `audited_original_info.cvm.cpu_core`（用于验证绿色增量）。
    - meta 接口 `/api/v1/woa/metas/ticket_types/list` 响应 `details` 中追加 `{ticket_type:"budget_declare", ticket_type_name:"预算申报"}`。

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | 回滚 |
|----|------|------|------|------|------|
| P0-01 | 类型列展示"预算申报" | 存在 budget_declare 单据 | 打开 单据管理→资源预测列表 | "类型"列该行显示"预算申报"（后端 `ticket_type_name`） | 无 |
| P0-02 | 类型筛选项含"预算申报" | meta 返回 budget_declare | 展开"类型"筛选下拉 | 选项中出现"预算申报" | 无 |
| P0-03 | 按"预算申报"筛选 | 列表含多种类型 | 选择"预算申报"并查询 | 列表仅显示预算申报类型单据 | 清空筛选 |
| P0-04 | 已审批核数绿色增量 | budget_declare 单据且已审批核数有增量 | 查看"CPU核数(已审批数)"列 | 数值为绿色（`#299e56`）正向 `+N` | 无 |

### P1 - 边界/回归（必测）

| ID | 场景 | 前置 | 操作 | 期望 |
|----|------|------|------|------|
| P1-01 | 原有类型无回归 | 存在 新增/调整/取消 单据 | 查看列表类型列、筛选、已审批核数颜色 | 与改动前一致：新增=绿色、调整/取消=红色；筛选项与展示正常 |
| P1-02 | 调整+部分失败特例保持 | 类型=调整 且 状态=部分失败 的单据 | 查看"CPU核数(已审批数)"列 | 显示 `--`（该特例逻辑未受影响） |
| P1-03 | 无该类型数据时 | 后端不返回 budget_declare | 打开列表 | 列表正常，无预算申报数据，无报错 |

### P2 - UI 细节（按需）

- [ ] 预算申报行"类型"列文案与其它类型排版一致，无截断/错位
- [ ] "已审批数"为 0 或缺失（NaN）时显示 `--`，不误染色
- [ ] 类型筛选多选/清空交互正常

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | Skipped | 用户确认直接推进到 done，未逐条手测回写 |
| P0-02 | Skipped | 同上；展示/筛选依赖后端返回 budget_declare |
| P0-03 | Skipped | 同上 |
| P0-04 | Skipped | 已审批核数绿色增量为纯前端逻辑，代码已核对 |
| P1-01 | Skipped | 原有类型逻辑未改动，理论无回归 |
| P1-02 | Skipped | "调整+部分失败"特例逻辑未改动 |
| P1-03 | Skipped | 无该类型数据分支未改动 |

- 执行日期: 2026-07-24
- 总体结论: PASS（由用户确认推进；改动为小范围前端适配，展示/筛选依赖后端返回 `budget_declare`，上线后以实际数据为准）
- 后续行动: 直接合入；待后端返回 budget_declare 后在实际数据上抽验 P0-01~04
