# feat-clblist-op-copy-add-clbname 测试清单

> 工作流: `feat-clblist-op-copy-add-clbname`
> 关联文档: Coding

## 验证范围

- 功能边界: CLB 负载均衡列表页批量复制下拉菜单新增“负载均衡名称”复制项
- 不在范围: Device 模块批量复制（独立组件）、侧边栏列表复制、其他页面

## 测试环境

- 前端入口: `<对应业务的测试环境地址，由执行人在测试时确认；不要把具体域名写入仓库>`
- 测试账号: `<test_account>`
- 数据准备:
  - 测试环境已有至少 2 条 CLB 负载均衡记录（含名称、ID、VIP、域名）

## 用例清单

### P0 - 主流程（必测）

| ID    | 场景                               | 前置                                       | 操作                                                   | 期望                                                                                | 回滚     |
| ----- | ---------------------------------- | ------------------------------------------ | ------------------------------------------------------ | ----------------------------------------------------------------------------------- | -------- |
| P0-01 | 批量复制下拉菜单出现“负载均衡名称” | 已进入 CLB 负载均衡列表页，至少有 2 条记录 | 勾选 2 条记录 → 点击“复制”下拉                         | 下拉菜单第一项为“负载均衡名称”，其后依次为“负载均衡 ID”“负载均衡 VIP”“负载均衡域名” | 无需回滚 |
| P0-02 | 复制名称内容与显示一致             | 已进入 CLB 列表页，至少有 1 条记录         | 勾选 1 条记录 → 复制 → 负载均衡名称 → 粘贴到文本编辑器 | 粘贴内容等于该条记录的负载均衡名称（即表格名称列显示的值）                          | 无需回滚 |
| P0-03 | 多选复制名称，换行分隔             | 已进入 CLB 列表页，至少有 2 条记录         | 勾选 2 条记录 → 复制 → 负载均衡名称 → 粘贴到文本编辑器 | 粘贴内容为 2 个名称，换行分隔，顺序与列表勾选顺序一致                               | 无需回滚 |

### P1 - 异常分支（必测）

| ID    | 场景                 | 前置                            | 操作               | 期望                                                    |
| ----- | -------------------- | ------------------------------- | ------------------ | ------------------------------------------------------- |
| P1-01 | 无选中时复制按钮禁用 | 进入 CLB 列表页，不勾选任何记录 | 鼠标悬浮“复制”按钮 | 按钮 disabled，不可点击，tooltip 提示“复制当前勾选数据” |
| P1-02 | 仅勾选后取消全部选中 | 先勾选 1 条再取消               | 查看“复制”按钮状态 | 恢复 disabled，下拉菜单不可展开                         |

### P2 - UI 细节（按需）

- [ ] “负载均衡名称”在菜单中位于“负载均衡 ID”上方
- [ ] 菜单项顺序: 名称 → ID → VIP → 域名（符合需求）
- [ ] 复制成功后下拉菜单自动关闭
- [ ] 复制成功后 Toast 提示“复制成功”

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID    | 结果 | 备注 |
| ----- | ---- | ---- |
| P0-01 |      |      |
| P0-02 |      |      |
| P0-03 |      |      |
| P1-01 |      |      |
| P1-02 |      |      |

- 执行日期: 2026-07-10
- 总体结论:
- 后续行动:
