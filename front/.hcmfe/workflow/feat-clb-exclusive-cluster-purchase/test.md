# Test — feat-clb-exclusive-cluster-purchase

> 工作流: `feat-clb-exclusive-cluster-purchase`（CLB 独占集群购买、单据与详情适配）
> 关联文档: `prd.md` / `design.md` / `api.md` / `coding.md`
> 关联单据: `1069995598138114049`（购买支持独占型规格）、`1069995598138114062`（单据展示独占集群参数）、`1069995598138114066`（CLB 详情独占集群字段）

## 验证范围

- 功能边界:
  1. 业务视角购买公网 CLB 时的独占型规格：四层/七层启用、集群标签与具体集群、空闲 IP、共享带宽包出口过滤、配置清单规格文案、提交参数。
  2. 负载均衡申请单详情的独占参数回显。
  3. CLB 资源详情「配置信息」的实例规格与独占集群字段。
- 不在范围:
  - 独占集群管理页（另一工作流）、CLB 列表实例规格列与筛选（另一工作流）。
  - 监听器 / 安全组 / 跨域配置等其它详情页签。
  - 内网 CLB（独占型仅公网可用）。
  - 后端接口与数据模型实现；本地未同步数据的一致性。

## 测试环境

- 前端入口:
  - 购买: `<测试环境 业务视角-负载均衡-购买负载均衡>`
  - 申请单详情: `<测试环境 单据管理-负载均衡申请单详情>`
  - 资源详情: `<测试环境 业务视角-负载均衡-CLB 详情-配置信息>`
  > 具体地址由执行人在测试时确认，不写入仓库。
- 测试账号: `<test_account>`（需为已分配独占集群的业务 / 账号 / 地域）
- 数据准备:
  - 业务 A（可独占）: 存在符合「公网 + 当前地域 + 可用区 + 运营商」的独占集群；至少 1 个 `TGW` 标签，标签下 ≥2 个集群，其中 ≥1 个集群存在空闲 IP，≥1 个集群空闲 IP 为 0；至少 1 个 `STGW` 标签。
  - 业务 B（不可独占）: 无任何可用独占集群。
  - 共享带宽包: 同一 `egress` 下 ≥2 个 `CREATED` 状态的包；存在 egress 不同的一组包（用于验证过滤/交集）；构造 >100 条同条件数据（用于验证全量分页）。
  - 已在库的独占型记录: 1 个独占型申请单（`content` 含 `exclusive=1`、`clusters[]`、`vip`）、1 个独占型 CLB（`extension.exclusive=1`、`extension.clusters[]`）；另备共享型、性能容量型各 1 条作为对照。
  - 异常分支: 用 `page.route` / mock 拦截接口返回 500 或空 `details`，不依赖真后端造异常。

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | covered-by | 回滚 |
|----|------|------|------|------|------------|------|
| P0-01 | 独占型规格可选 | 业务 A，公网，账号/地域/可用区/运营商已填 | 打开「负载均衡规格类型」下拉 | 选项顺序为 共享型 / 性能容量型 / 独占型，独占型可选；选择后展示「独占集群」配置区，四层与七层复选框默认均未勾选 | | 切回共享型 |
| P0-02 | 四层随机集群提交 | 已选独占型 | 勾选四层 → 选四层标签 → 具体集群选「随机分配」→ 保存 → 提交 | 提交参数 `exclusive=1`、`sla_type=""`、`cloud_cluster_ids` = 该标签下全部 `cloud_cluster_id`、`vip=""`、不传 `cluster_tag`；配置清单规格列显示「独占型」 | | 取消单据 |
| P0-03 | 四层指定集群 + 指定 IP 提交 | 已选独占型，目标集群有空闲 IP | 勾选四层 → 选标签 → 选具体集群 → IP 选具体值 → 保存 → 提交 | `cloud_cluster_ids=[该集群 cloud_cluster_id]`、`vip=<所选 IP>`、`exclusive=1`、`sla_type=""` | | 取消单据 |
| P0-04 | 仅七层提交 | 已选独占型 | 勾选七层 → 选七层标签 → 保存 → 提交 | `cluster_tag=<七层标签>`、`exclusive=1`；`cloud_cluster_ids` 与 `vip` 均不出现 | | 取消单据 |
| P0-05 | 四层 + 七层同时提交 | 已选独占型 | 两层均勾选并各自选好标签（四层再选集群与 IP）→ 提交 | `cloud_cluster_ids` 与 `cluster_tag` 同时存在，`vip` 按四层选择输出 | | 取消单据 |
| P0-06 | 带宽包按出口过滤 | 已选独占型，网络计费模式=共享带宽包 | ① 四层选具体集群 ② 四层改「随机分配」 ③ 四层+七层同时启用 | ① 候选包 `egress` 仅等于该集群 `egress` ② 候选为标签下全部集群 `egress` 去重集合 ③ 候选为两层出口集合的**交集**；每种情况下候选都覆盖全部分页（>100 条不漏） | | 重新选择计费模式 |
| P0-07 | 申请单详情独占回显 | 存在独占型申请单 | 打开负载均衡申请单详情 | 「负载均衡规格类型」显示「独占型」，其后依次显示 四层集群标签 / 四层集群名称 / 四层集群 IP / 七层集群标签，取值来自申请时的 `content`（非交付后资源） | | 关闭详情 |
| P0-08 | CLB 详情独占回显 | 存在独占型 CLB | 打开 CLB 详情-配置信息 | 实例规格显示「独占型」，其后显示 四层集群标签 / 四层集群名称 / 四层集群 IP / 七层集群标签；其中四层集群 IP 与上方「负载均衡 VIP」完全一致 | | 关闭详情 |
| P0-09 | 非独占不展示独占字段 | 共享型、性能容量型各 1 条申请单与 1 条 CLB | 分别打开申请单详情与 CLB 详情 | 不出现任何独占集群字段；实例规格分别显示「共享型」「性能容量档位名」 | | 关闭详情 |

### P1 - 异常分支（必测）

| ID | 场景 | 前置 | 操作 | 期望 | covered-by |
|----|------|------|------|------|------------|
| P1-01 | 两层均未启用 | 独占型配置区 | 不勾选任何层，直接点击「保存」 | 「独占集群」区域红字提示「请至少启用并完整配置一个独占集群」，不写入配置清单 | |
| P1-02 | 启用层配置不完整 | 独占型配置区 | 勾选四层但不选标签 / 不选具体集群 / 不选 IP，点击「保存」 | 同上校验拦截；勾选七层但不选七层标签同样被拦截 | |
| P1-03 | 具体集群无空闲 IP | 存在空闲 IP 为 0 的 TGW 集群 | 四层选该具体集群 | Toast 警告「该集群暂无空闲 IP，请选择其它集群」，该集群与 IP 选择被清空，无法保存为有效配置 | |
| P1-04 | 独占集群查询失败 | 拦截标签接口返回 500 | 打开购买表单 / 切换地域 | 独占型选项不出现（已选独占型则自动回退为共享型）；若计费模式为共享带宽包也不得提交 | |
| P1-05 | 出口集合为空 / 带宽包加载失败 | 当前启用层无法解析出任何 `egress`，或拦截带宽包接口 500 | 观察共享带宽包选择器 | 出口为空时列表为空且不发请求；接口失败时列表为空；两种情况下共享带宽包计费均不可保存 | |
| P1-06 | 上游条件变化清空选择 | 已选好四层标签/集群/IP、已选带宽包 | 切换账号、地域、可用区或运营商 | 四层标签/集群/IP、七层标签全部清空并重新查询标签；已选带宽包被清空 | |
| P1-07 | 取消勾选保留值 | 已选好四层标签/集群/IP | 取消勾选四层，再重新勾选 | 取消后控件置灰、值可见但保留；保存与提交、出口计算均不包含该层；重新勾选后原值恢复生效（指定集群会重新拉取空闲 IP） | |
| P1-08 | 未勾选层不得出现在请求中 | 已选独占型 | 仅勾选七层（四层先选好再取消勾选）→ 提交 | 请求中不出现 `cloud_cluster_ids` 与 `vip`，仅出现 `cluster_tag` | |
| P1-09 | 非独占不得携带独占参数 | 先在独占型下配置好两层 | 切回共享型 / 性能容量型 → 保存 → 提交 | 请求中不出现 `cluster_tag`、`cloud_cluster_ids`、`vip`；`exclusive=0`；共享带宽包选择器恢复原有非独占行为 | |
| P1-10 | 询价不被独占字段污染 | 已选独占型并配置完成 | 触发询价（改带宽上限等） | 询价请求不包含 `enable_l4`、`enable_l7`、`l4_cluster_tag`、`l4_cluster_id`、`l4_vip`、`exclusive_cluster_tags`，也不带 `bandwidth_package_id`；询价前置条件与共享型一致 | |
| P1-11 | 申请单 `content` 解析失败 | mock `content` 为非法 JSON 或缺字段 | 打开申请单详情 | 页面不白屏，其它参数信息正常展示，独占字段按缺失显示空值 | |
| P1-12 | 详情字段缺失 | mock `clusters` 缺 `TGW`/`STGW` 元素，或 `cluster_name`、`vip` 为空 | 打开申请单详情 / CLB 详情 | 对应字段显示统一空值（`--`），其余内容正常；七层标签缺失时回退用 `cluster_tag` 展示 | |

### P2 - UI 细节（按需）

- [ ] 未勾选层的三个选择器均置灰，勾选后立即可编辑；标签/空闲 IP 加载时选择器显示 loading
- [ ] 四层行「复选框 + 标签 + 集群 + IP」横向排列，宽度与表单既有 Select 一致，不换行错位
- [ ] 配置清单「规格」列在 独占型 / 性能容量型（档位名）/ 共享型 三种情况下文案正确
- [ ] 带宽包全量加载期间选择器 loading；数据 >100 条时候选不因分页遗漏
- [ ] 申请单详情与 CLB 详情新增字段与既有字段网格对齐，空值统一为 `--`
- [ ] 1280 / 1440 / 1920 三种宽度下独占集群行与详情字段无横向溢出
- [ ] 无创建权限时「提交」按钮禁用并悬浮提示（沿用现有权限逻辑）
- [ ] 内网 CLB、无可用独占集群业务下，规格类型下拉不含独占型

## 自动化覆盖（本次执行方式：E2E + mock）

- 用例文件：`<dataDir>/workflow/feat-clb-exclusive-cluster-purchase/e2e.spec.ts`（同目录，共 23 条，与本文 P0/P1 编号一一对应）
- mock：`front/e2e/mocks/data.ts` + `front/e2e/mocks/api.ts`（全部接口拦截，无真实账号 / 域名 / 凭据）
- 运行：`npm run e2e -- .hcmfe/workflow/feat-clb-exclusive-cluster-purchase/e2e.spec.ts`
- 说明：自动化仅覆盖 mock 路径；真实后端字段形态与竞态（旧响应不回写）需在联调环境补测。

## 真实环境联调清单（mock 覆盖不到，推进 done 前必做）

> 前置：后端已部署本次接口变更；业务已分配至少一个 TGW 标签（含 ≥2 个集群，其中 ≥1 个有空闲 IP）与一个 STGW 标签；带宽包存在跨页数据（>100 条）。

| ID | 确认项 | 为什么 mock 覆盖不到 | 关联 |
|----|--------|----------------------|------|
| L-01 | 标签聚合接口按 `zones` 数组接收并按可用区/运营商正确筛选 | mock 只回固定数据，不校验入参语义 | api.md §2 文档差异 |
| L-02 | 仅四层不传 `cluster_tag`、仅七层不传 `cloud_cluster_ids`、四七层同时传，三种组合后端均接受 | 后端接线口径与 PR 文字冲突，只有真实调用能证伪 | api.md §4 文档差异 |
| L-03 | 四层随机集群传该标签下全部候选 `cloud_cluster_ids` + `vip: ""` 后端接受 | 同上，PR 空闲 VIP 文档与购买文档矛盾 | api.md §4 文档差异 |
| L-04 | 空闲 VIP 被并发占用 / 标签无权 / 带宽包出口不匹配时的错误码与页面提示 | 需真实错误码（`InvalidParameter` / `PermissionDenied`） | api.md §8 |
| L-05 | 共享带宽包跨多页全量读取与出口交集（真实 `total_count` / 分页键） | mock 一次性返回全部，无法验证真实分页 | api.md §5 |
| L-06 | 申请单详情的业务视角 / 资源视角外层响应形态，`content.clusters` 的 TGW/STGW 数据；随机分配时 `vip`、`cluster_name` 为空 | 两个视角外层包装不同 | api.md §6 |
| L-07 | CLB 详情 `extension.clusters` 实际字段；四层集群 IP 与页面「负载均衡VIP」一致，覆盖 IPv4 / IPv6 / 仅内网 / 全空 | 需真实资源 | api.md §7 |
| L-08 | 独占型专属字段在真实 `sla_type` 语义下不互相污染（独占型空串、性能容量型档位、共享型空串） | 真实数据同时存在三种形态 | prd §5.8 |
| L-09 | 旧数据兼容：历史 CLB / 申请单缺少 `exclusive`、`clusters` 时不报错、按非独占展示 | mock 只有构造数据 | coding.md 单据 2/3 |
| L-10 | 表单条件切换后的竞态：快速切换账号/地域/可用区/标签，旧标签、空闲 IP、带宽包响应不回写 | 需真实网络时延才能触发 | prd §5.1/§5.5 |

- 联调结论同样回填本表（PASS / FAIL + 备注），FAIL 项按 `failure-triage` 判定改用例还是改代码。

## 缺陷与修复（D-01 — 已在 coding 阶段修复并复验）

**D-01 独占型配置无法保存（P0 缺陷，曾阻塞单据 1 的全部提交路径）**

- 现象：选好独占型并完整配置四层（标签 + 随机集群 + 随机 IP）后点击「保存」，「独占集群」字段报错「独占集群不能为空」，侧滑不关闭、配置清单不新增；即使除独占集群外的必填项全部填好，唯一的报错仍是这一条。
- 根因（源码定位）：`src/views/service/service-apply/clb/hooks/useRenderForm.tsx` 中「独占集群」`FormItem` 同时设置了 `required: true` 与 `property: 'exclusive_config'`，而 `exclusive_config` 并不是 `ApplyClbModel` 上的字段，bkui 的表单必填规则始终判定为空并给出默认文案，自定义校验器的文案「请至少启用并完整配置一个独占集群」永远不生效。
- 影响：`P0-02`~`P0-05`（四层/七层/合并/具体集群的提交参数）全部无法到达提交，`P1-01` 的实际提示文案与设计意图不一致。
- 修复（coding 阶段）：移除该 `FormItem` 的 `required: true`；把自定义校验迁到 `FormItem` 的项级 `rules`（BKUI `FormItem` 支持 `Array<IFormItemRule>`，已核对组件文档）；`ClbFormItemOption` 增加可选 `rules` 并透传。详见 `coding.md` 的「D-01 修复记录」。
- 视觉副作用：该字段不再显示必填星号（BKUI 无独立控制星号的属性）。如需保留星号，可让 `exclusive_config` 落到真实模型字段，请需求方确认。
- 复验：修复后重跑同一 spec，**22 passed / 0 failed**；此前 Blocked 的 `P0-02`~`P0-05`、`P1-02`、`P1-08`、`P1-09` 全部补齐并通过。
- 原始失败证据（保留追溯）：`e2e/test-results/...四层随机集群可保存并提交标签下全部候选集群与空-VIP/test-failed-1.png`。

## 验证结论

> 每条用例填写：PASS / FAIL / Skipped / Blocked + 简要说明（FAIL 必须附复现步骤、错误截图链接或日志片段）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | PASS | 自动化：`P0-01`；具备可用独占集群时规格类型出现「独占型」，选中后展示四层/七层启用项 |
| P0-02 | PASS | 自动化：`P0-02`；四层随机集群提交 `cloud_cluster_ids` 为该标签下全部候选、`vip=""`、不传 `cluster_tag` |
| P0-03 | PASS | 自动化：`P0-03`；四层指定集群 + 指定 IP 提交 `cloud_cluster_ids=[qcs-l4-a]`、`vip=10.0.0.21` |
| P0-04 | PASS | 自动化：`P0-04`；仅七层提交 `cluster_tag`，不输出四层字段 |
| P0-05 | PASS | 自动化：`P0-05`；四层 + 七层同时启用时两类字段同时输出 |
| P0-06 | PASS | 自动化：`P0-06`；四层指定集群（egress-a）时候选仅含 egress-a 的包，egress-b 的包不出现 |
| P0-07 | PASS | 自动化：`P0-07`；独占型申请单展示四层标签/名称/IP/七层标签，且排在「负载均衡规格类型」之后 |
| P0-08 | PASS | 自动化：`P0-08`；实例规格=独占型，四层集群 IP 与「负载均衡VIP」取值一致 |
| P0-09 | PASS | 自动化：`P0-09`（两条）；共享型显示「共享型」、性能容量型显示「超强型2规格」，均不渲染独占字段 |
| P1-01 | PASS | 自动化：`P1-01`；两层均未启用时保存被拦截、配置清单不新增，提示「请至少启用并完整配置一个独占集群」（D-01 修复后文案已符合预期） |
| P1-02 | PASS | 自动化：`P1-02`；启用层配置不完整时同样被「请至少启用并完整配置一个独占集群」拦截 |
| P1-03 | PASS | 自动化：`P1-03`；选到无空闲 IP 的具体集群提示「该集群暂无空闲 IP，请选择其它集群」且集群选择被清空 |
| P1-04 | PASS | 自动化：`P1-04`；标签接口 500 时规格类型只提供「共享型 / 性能容量型」，不出现「独占型」 |
| P1-05 | PASS | 自动化：`P1-05`；仅七层且出口集合为空时共享带宽包候选为空且不发请求 |
| P1-06 | PASS | 自动化：`P1-06`；切换可用区后重新选独占型，上一轮四层标签保留值已被清空 |
| P1-07 | PASS | 自动化：`P1-07`；取消勾选后选择器置灰且保留值仍在，重新勾选后恢复 |
| P1-08 | PASS | 自动化：`P1-08`；取消勾选的四层不出现在提交参数中（`cloud_cluster_ids`/`vip` 均未输出） |
| P1-09 | PASS | 自动化：`P1-09`；切回共享型后 `exclusive=0`，且不携带 `cluster_tag`/`cloud_cluster_ids`/`vip` |
| P1-10 | PASS | 自动化：`P1-10`；切回共享型后的询价请求不含 `enable_l4/enable_l7/l4_*`、`exclusive_cluster_tags`，也不带 `bandwidth_package_id` |
| P1-11 | PASS | 自动化：`P1-11`；`content` 非法 JSON 时不白屏，其它参数正常、独占区块不渲染 |
| P1-12 | PASS | 自动化：`P1-12`（两条）；随机分配时四层集群名称/IP 显示 `--`；CLB 详情缺字段同样显示 `--` 且不阻断其它配置信息 |

- 执行日期: 2026-09-18
- 执行方式: E2E（Playwright + mock 数据），工作流内 spec 单跑
- 本轮结果: **23 passed / 0 failed**（D-01 修复后复验，含此前 Blocked 的全部用例）
- 报告路径: `front/e2e/playwright-report`（失败证据归档在 `front/e2e/test-results`）
- 总体结论: PASS —— 三张单据的展示与提交路径均通过
- 后续行动: 待用户确认后 approve test；如需留脚本可另走 `verify-e2e` 固化 P0；合入前补齐真实环境联调（见 api.md §8 联调重点）

